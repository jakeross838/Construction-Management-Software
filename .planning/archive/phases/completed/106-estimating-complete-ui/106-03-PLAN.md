# Phase 106-03: Estimating - Hierarchical UI Rendering

## Overview

Implement the frontend UI for hierarchical estimates: collapsible phases with groups, subgroups, and line items. Includes template selection on create and drag-drop reordering.

---

## Context

**Dependencies:**
- 106-01: Schema with phases/groups/subgroups
- 106-02: API endpoints for hierarchy CRUD

**UI Pattern:**
```
Estimate: Kitchen Remodel
├── [▼] Phase: Pre-Construction ($2,500)
│   ├── [▼] Group: Permits & Fees ($2,500)
│   │   ├── [▼] Subgroup: Building Permit ($1,500)
│   │   │   └── Line: Permit Fee - $1,500
│   │   └── [▼] Subgroup: Utility Connections ($1,000)
│   │       └── Line: Utility Connect - $1,000
├── [▼] Phase: Framing ($15,000)
│   └── ...
```

---

## Tasks

### Task 1: Update Estimate Detail to Show Hierarchy

**Goal:** Render phases/groups/subgroups as collapsible accordion.

**HTML Structure:**
```html
<div class="estimate-hierarchy">
  <!-- Phases -->
  <div class="estimate-phase" data-phase-id="uuid">
    <div class="phase-header" onclick="togglePhase(this)">
      <span class="collapse-icon">▼</span>
      <span class="phase-name">Framing</span>
      <span class="phase-subtotal">$15,000.00</span>
      <div class="phase-actions">
        <button class="btn btn-ghost btn-xs" onclick="editPhase(event, 'uuid')">Edit</button>
        <button class="btn btn-ghost btn-xs" onclick="addGroup('uuid')">+ Group</button>
      </div>
    </div>
    <div class="phase-content">
      <!-- Groups -->
      <div class="estimate-group" data-group-id="uuid">
        <div class="group-header" onclick="toggleGroup(this)">
          <span class="collapse-icon">▼</span>
          <span class="group-name">Wall Framing</span>
          <span class="group-subtotal">$8,500.00</span>
          <div class="group-actions">
            <button class="btn btn-ghost btn-xs" onclick="editGroup(event, 'uuid')">Edit</button>
            <button class="btn btn-ghost btn-xs" onclick="addSubgroup('uuid')">+ Subgroup</button>
          </div>
        </div>
        <div class="group-content">
          <!-- Subgroups -->
          <div class="estimate-subgroup" data-subgroup-id="uuid">
            <div class="subgroup-header" onclick="toggleSubgroup(this)">
              <span class="collapse-icon">▼</span>
              <span class="subgroup-name">Exterior Walls</span>
              <span class="subgroup-subtotal">$5,200.00</span>
              <div class="subgroup-actions">
                <button class="btn btn-ghost btn-xs" onclick="editSubgroup(event, 'uuid')">Edit</button>
                <button class="btn btn-ghost btn-xs" onclick="addLineItem('uuid')">+ Item</button>
              </div>
            </div>
            <div class="subgroup-content">
              <!-- Line Items Table -->
              <table class="data-table estimate-lines-table">
                <thead>
                  <tr>
                    <th style="width: 30px"></th>
                    <th>Description</th>
                    <th>Cost Code</th>
                    <th class="col-right">Qty</th>
                    <th>Unit</th>
                    <th class="col-right">Unit Cost</th>
                    <th class="col-right">Amount</th>
                    <th style="width: 80px"></th>
                  </tr>
                </thead>
                <tbody>
                  <!-- Line items rendered here -->
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

---

### Task 2: Add CSS for Hierarchy

**Goal:** Style the collapsible hierarchy with proper indentation.

**Add to styles.css:**
```css
/* ============================================================
   ESTIMATE HIERARCHY
   ============================================================ */

.estimate-hierarchy {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

/* Phase Level */
.estimate-phase {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 8px;
  overflow: hidden;
}

.phase-header {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 16px;
  background: var(--bg-card-elevated);
  cursor: pointer;
  user-select: none;
}

.phase-header:hover {
  background: var(--bg-hover);
}

.phase-name {
  font-weight: 600;
  font-size: 1rem;
  flex: 1;
}

.phase-subtotal {
  font-weight: 600;
  font-family: 'SF Mono', monospace;
  color: var(--text-primary);
}

.phase-actions {
  display: flex;
  gap: 4px;
  opacity: 0;
  transition: opacity 0.2s;
}

.phase-header:hover .phase-actions {
  opacity: 1;
}

.phase-content {
  padding: 8px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.estimate-phase.collapsed .phase-content {
  display: none;
}

/* Group Level */
.estimate-group {
  background: var(--bg-primary);
  border: 1px solid var(--border);
  border-radius: 6px;
  margin-left: 16px;
}

.group-header {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 14px;
  cursor: pointer;
  user-select: none;
}

.group-header:hover {
  background: var(--bg-card);
}

.group-name {
  font-weight: 500;
  flex: 1;
}

.group-subtotal {
  font-weight: 500;
  font-family: 'SF Mono', monospace;
  color: var(--text-secondary);
}

.group-actions {
  display: flex;
  gap: 4px;
  opacity: 0;
  transition: opacity 0.2s;
}

.group-header:hover .group-actions {
  opacity: 1;
}

.group-content {
  padding: 8px;
}

.estimate-group.collapsed .group-content {
  display: none;
}

/* Subgroup Level */
.estimate-subgroup {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 4px;
  margin-left: 16px;
}

.subgroup-header {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 12px;
  cursor: pointer;
  user-select: none;
  font-size: 0.9rem;
}

.subgroup-header:hover {
  background: var(--bg-card-elevated);
}

.subgroup-name {
  flex: 1;
  color: var(--text-secondary);
}

.subgroup-subtotal {
  font-family: 'SF Mono', monospace;
  font-size: 0.85rem;
  color: var(--text-secondary);
}

.subgroup-actions {
  display: flex;
  gap: 4px;
  opacity: 0;
  transition: opacity 0.2s;
}

.subgroup-header:hover .subgroup-actions {
  opacity: 1;
}

.subgroup-content {
  padding: 8px 12px;
}

.estimate-subgroup.collapsed .subgroup-content {
  display: none;
}

/* Collapse Icon */
.collapse-icon {
  width: 16px;
  text-align: center;
  color: var(--text-secondary);
  transition: transform 0.2s;
}

.collapsed .collapse-icon {
  transform: rotate(-90deg);
}

/* Line Items Table within Subgroup */
.estimate-lines-table {
  font-size: 0.9rem;
}

.estimate-lines-table th {
  padding: 8px;
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.estimate-lines-table td {
  padding: 8px;
}

.line-item-row.has-catalog {
  background: rgba(88, 166, 255, 0.05);
}

.line-item-row .catalog-badge {
  font-size: 0.7rem;
  background: var(--accent-blue);
  color: white;
  padding: 2px 6px;
  border-radius: 3px;
  margin-left: 8px;
}
```

---

### Task 3: Implement JavaScript Rendering

**Goal:** Render estimate hierarchy from API data.

```javascript
function renderEstimateHierarchy(estimate) {
  const container = document.getElementById('estimateHierarchy');
  if (!container) return;

  if (!estimate.phases || estimate.phases.length === 0) {
    container.innerHTML = `
      <div class="empty-state">
        <h3>No phases yet</h3>
        <p>Apply a template or add phases manually to structure your estimate.</p>
        <div style="display: flex; gap: 12px; justify-content: center; margin-top: 16px;">
          <button class="btn btn-primary" onclick="openTemplateSelector()">Apply Template</button>
          <button class="btn btn-ghost" onclick="addPhase()">+ Add Phase</button>
        </div>
      </div>
    `;
    return;
  }

  container.innerHTML = estimate.phases.map(phase => `
    <div class="estimate-phase" data-phase-id="${phase.id}">
      <div class="phase-header" onclick="togglePhase(this)">
        <span class="collapse-icon">▼</span>
        <span class="phase-name">${escapeHtml(phase.name)}</span>
        <span class="phase-subtotal">${formatCurrency(phase.subtotal)}</span>
        <div class="phase-actions">
          <button class="btn btn-ghost btn-xs" onclick="editPhase(event, '${phase.id}')">Edit</button>
          <button class="btn btn-ghost btn-xs" onclick="addGroup('${phase.id}')">+ Group</button>
          <button class="btn btn-ghost btn-xs text-danger" onclick="deletePhase(event, '${phase.id}')">Delete</button>
        </div>
      </div>
      <div class="phase-content">
        ${(phase.groups || []).map(group => renderGroup(group)).join('')}
        ${phase.groups?.length === 0 ? `
          <div class="empty-state-inline">
            <span>No groups</span>
            <button class="btn btn-ghost btn-xs" onclick="addGroup('${phase.id}')">+ Add Group</button>
          </div>
        ` : ''}
      </div>
    </div>
  `).join('');

  // Update total
  const total = estimate.phases.reduce((sum, p) => sum + parseFloat(p.subtotal || 0), 0);
  document.getElementById('estimateTotalAmount').textContent = formatCurrency(total);
}

function renderGroup(group) {
  return `
    <div class="estimate-group" data-group-id="${group.id}">
      <div class="group-header" onclick="toggleGroup(this)">
        <span class="collapse-icon">▼</span>
        <span class="group-name">${escapeHtml(group.name)}</span>
        <span class="group-subtotal">${formatCurrency(group.subtotal)}</span>
        <div class="group-actions">
          <button class="btn btn-ghost btn-xs" onclick="editGroup(event, '${group.id}')">Edit</button>
          <button class="btn btn-ghost btn-xs" onclick="addSubgroup('${group.id}')">+ Subgroup</button>
          <button class="btn btn-ghost btn-xs text-danger" onclick="deleteGroup(event, '${group.id}')">Delete</button>
        </div>
      </div>
      <div class="group-content">
        ${(group.subgroups || []).map(sg => renderSubgroup(sg)).join('')}
        ${group.subgroups?.length === 0 ? `
          <div class="empty-state-inline">
            <span>No subgroups</span>
            <button class="btn btn-ghost btn-xs" onclick="addSubgroup('${group.id}')">+ Add Subgroup</button>
          </div>
        ` : ''}
      </div>
    </div>
  `;
}

function renderSubgroup(subgroup) {
  const lines = subgroup.line_items || [];
  return `
    <div class="estimate-subgroup" data-subgroup-id="${subgroup.id}">
      <div class="subgroup-header" onclick="toggleSubgroup(this)">
        <span class="collapse-icon">▼</span>
        <span class="subgroup-name">${escapeHtml(subgroup.name)}</span>
        <span class="subgroup-subtotal">${formatCurrency(subgroup.subtotal)}</span>
        <div class="subgroup-actions">
          <button class="btn btn-ghost btn-xs" onclick="editSubgroup(event, '${subgroup.id}')">Edit</button>
          <button class="btn btn-ghost btn-xs" onclick="addLineItem('${subgroup.id}')">+ Item</button>
          <button class="btn btn-ghost btn-xs text-danger" onclick="deleteSubgroup(event, '${subgroup.id}')">Delete</button>
        </div>
      </div>
      <div class="subgroup-content">
        ${lines.length > 0 ? `
          <table class="data-table estimate-lines-table">
            <thead>
              <tr>
                <th style="width: 30px"></th>
                <th>Description</th>
                <th>Cost Code</th>
                <th class="col-right">Qty</th>
                <th>Unit</th>
                <th class="col-right">Unit Cost</th>
                <th class="col-right">Amount</th>
                <th style="width: 80px"></th>
              </tr>
            </thead>
            <tbody>
              ${lines.map(line => renderLineItem(line)).join('')}
            </tbody>
          </table>
        ` : `
          <div class="empty-state-inline">
            <span>No items</span>
            <button class="btn btn-ghost btn-xs" onclick="addLineItem('${subgroup.id}')">+ Add Item</button>
          </div>
        `}
      </div>
    </div>
  `;
}

function renderLineItem(line) {
  const hasCatalog = !!line.catalog_item_id;
  return `
    <tr class="line-item-row ${hasCatalog ? 'has-catalog' : ''}" data-line-id="${line.id}">
      <td>
        <span class="drag-handle" title="Drag to reorder">⋮⋮</span>
      </td>
      <td>
        ${escapeHtml(line.description)}
        ${hasCatalog ? `<span class="catalog-badge">Catalog</span>` : ''}
      </td>
      <td>${line.cost_code?.code || '-'}</td>
      <td class="col-right">${line.quantity}</td>
      <td>${line.unit || '-'}</td>
      <td class="col-right">${formatCurrency(line.unit_cost)}</td>
      <td class="col-right">${formatCurrency(line.amount)}</td>
      <td>
        <button class="btn btn-ghost btn-xs" onclick="editLineItem('${line.id}')">Edit</button>
        <button class="btn btn-ghost btn-xs text-danger" onclick="deleteLineItem('${line.id}')">Delete</button>
      </td>
    </tr>
  `;
}
```

---

### Task 4: Implement Collapse/Expand Functions

**Goal:** Toggle collapsed state for phases, groups, subgroups.

```javascript
function togglePhase(header) {
  const phase = header.closest('.estimate-phase');
  phase.classList.toggle('collapsed');
  saveCollapseState();
}

function toggleGroup(header) {
  const group = header.closest('.estimate-group');
  group.classList.toggle('collapsed');
  saveCollapseState();
}

function toggleSubgroup(header) {
  const subgroup = header.closest('.estimate-subgroup');
  subgroup.classList.toggle('collapsed');
  saveCollapseState();
}

function expandAll() {
  document.querySelectorAll('.estimate-phase, .estimate-group, .estimate-subgroup')
    .forEach(el => el.classList.remove('collapsed'));
  saveCollapseState();
}

function collapseAll() {
  document.querySelectorAll('.estimate-phase, .estimate-group, .estimate-subgroup')
    .forEach(el => el.classList.add('collapsed'));
  saveCollapseState();
}

// Save collapse state to localStorage per estimate
function saveCollapseState() {
  if (!currentEstimate) return;
  const state = {};

  document.querySelectorAll('.estimate-phase').forEach(el => {
    state[`phase-${el.dataset.phaseId}`] = el.classList.contains('collapsed');
  });
  document.querySelectorAll('.estimate-group').forEach(el => {
    state[`group-${el.dataset.groupId}`] = el.classList.contains('collapsed');
  });
  document.querySelectorAll('.estimate-subgroup').forEach(el => {
    state[`subgroup-${el.dataset.subgroupId}`] = el.classList.contains('collapsed');
  });

  localStorage.setItem(`estimate-collapse-${currentEstimate.id}`, JSON.stringify(state));
}

function restoreCollapseState() {
  if (!currentEstimate) return;
  const saved = localStorage.getItem(`estimate-collapse-${currentEstimate.id}`);
  if (!saved) return;

  const state = JSON.parse(saved);

  Object.entries(state).forEach(([key, collapsed]) => {
    const [type, id] = key.split('-');
    const selector = type === 'phase' ? '.estimate-phase' :
                    type === 'group' ? '.estimate-group' : '.estimate-subgroup';
    const el = document.querySelector(`${selector}[data-${type}-id="${id}"]`);
    if (el && collapsed) el.classList.add('collapsed');
  });
}
```

---

### Task 5: Template Selection Modal

**Goal:** Modal to select template when creating estimate.

```javascript
async function openTemplateSelector() {
  const templates = await loadTemplates();

  const modal = document.getElementById('templateModal');
  const list = document.getElementById('templateList');

  list.innerHTML = templates.map(t => `
    <div class="template-option ${t.is_default ? 'default' : ''}"
         onclick="selectTemplate('${t.id}')">
      <div class="template-info">
        <h4>${escapeHtml(t.name)}</h4>
        <p>${escapeHtml(t.description || '')}</p>
        <span class="template-meta">${t.project_type} • ${countPhases(t)} phases</span>
      </div>
      ${t.is_default ? '<span class="badge badge-info">Default</span>' : ''}
    </div>
  `).join('');

  modal.style.display = 'flex';
  modal.classList.add('show');
}

async function selectTemplate(templateId) {
  if (!currentEstimate) {
    showToast('Please create an estimate first', 'error');
    return;
  }

  try {
    const response = await fetch(`/api/estimates/${currentEstimate.id}/apply-template`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ template_id: templateId })
    });

    if (!response.ok) {
      const err = await response.json();
      throw new Error(err.error || 'Failed to apply template');
    }

    const estimate = await response.json();
    currentEstimate = estimate;
    renderEstimateHierarchy(estimate);

    closeTemplateModal();
    showToast('Template applied successfully', 'success');
  } catch (err) {
    console.error('Error applying template:', err);
    showToast(err.message, 'error');
  }
}

function countPhases(template) {
  return template.phases?.length || 0;
}
```

---

### Task 6: Add Phase/Group/Subgroup Inline

**Goal:** Quick-add modals for hierarchy items.

```javascript
async function addPhase() {
  const name = prompt('Phase name:');
  if (!name) return;

  try {
    const response = await fetch(`/api/estimates/${currentEstimate.id}/phases`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name })
    });

    if (!response.ok) throw new Error('Failed to add phase');

    await refreshEstimate();
    showToast('Phase added', 'success');
  } catch (err) {
    console.error('Error adding phase:', err);
    showToast(err.message, 'error');
  }
}

async function addGroup(phaseId) {
  const name = prompt('Group name:');
  if (!name) return;

  try {
    const response = await fetch(`/api/estimates/phases/${phaseId}/groups`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name })
    });

    if (!response.ok) throw new Error('Failed to add group');

    await refreshEstimate();
    showToast('Group added', 'success');
  } catch (err) {
    console.error('Error adding group:', err);
    showToast(err.message, 'error');
  }
}

async function addSubgroup(groupId) {
  const name = prompt('Subgroup name:');
  if (!name) return;

  try {
    const response = await fetch(`/api/estimates/groups/${groupId}/subgroups`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name })
    });

    if (!response.ok) throw new Error('Failed to add subgroup');

    await refreshEstimate();
    showToast('Subgroup added', 'success');
  } catch (err) {
    console.error('Error adding subgroup:', err);
    showToast(err.message, 'error');
  }
}

async function refreshEstimate() {
  if (!currentEstimate) return;

  const response = await fetch(`/api/estimates/${currentEstimate.id}`);
  currentEstimate = await response.json();
  renderEstimateHierarchy(currentEstimate);
  restoreCollapseState();
}
```

---

## Verification

After implementation:
1. Estimate detail shows collapsible phase hierarchy
2. Phases expand/collapse with arrow click
3. Groups and subgroups collapse independently
4. Collapse state persists in localStorage
5. Subtotals display at each level
6. "+ Add" buttons appear on hover
7. Template selector shows available templates
8. Applying template creates full structure
9. Empty states show when no items

---

## Files Modified

- `public/css/styles.css` - Add ~150 lines for hierarchy styles
- `public/js/estimates-budget.js` - Add ~300 lines for rendering and interactions
- `public/estimates-budget.html` - Add template modal HTML (~50 lines)

---

## Success Criteria

- [ ] Phases render as collapsible sections
- [ ] Groups render nested within phases
- [ ] Subgroups render nested within groups
- [ ] Line items render as table within subgroups
- [ ] Collapse/expand works at all levels
- [ ] Collapse state persists per estimate
- [ ] Template selector modal works
- [ ] Add phase/group/subgroup works
- [ ] Subtotals display correctly at each level
