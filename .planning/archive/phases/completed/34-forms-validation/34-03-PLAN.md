---
phase: 34-forms-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/styles.css
autonomous: true
must_haves:
  truths:
    - "Form layouts have consistent grid patterns"
    - "Form sections have uniform styling"
    - "Multi-column forms work responsively"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Unified form layout CSS"
  key_links: []
---

<objective>
Standardize form layout patterns (rows, grids, sections).

Purpose: Create consistent multi-column and sectioned form layouts.
Output: Unified CSS for form rows, grids, and sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify form row/grid layouts</name>
  <files>public/css/styles.css</files>
  <action>
Consolidate form layout classes:

1. Find existing .form-row, .form-grid, .form-column definitions
2. Create unified layout system:

```css
/* Form row - standard two-column layout */
.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
}

/* Form grid - flexible multi-column */
.form-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1rem;
}

/* Form grid variants */
.form-grid-2 {
  grid-template-columns: 1fr 1fr;
}

.form-grid-3 {
  grid-template-columns: 1fr 1fr 1fr;
}

/* Flex column sizing */
.flex-1 { flex: 1; }
.flex-2 { flex: 2; }
.flex-3 { flex: 3; }

/* Responsive breakpoint */
@media (max-width: 768px) {
  .form-row,
  .form-grid,
  .form-grid-2,
  .form-grid-3 {
    grid-template-columns: 1fr;
  }
}
```
  </action>
  <verify>Grep for "form-row" - should have grid layout</verify>
  <done>Form row/grid layouts unified</done>
</task>

<task type="auto">
  <name>Task 2: Standardize form sections</name>
  <files>public/css/styles.css</files>
  <action>
Consolidate form section styling:

1. Find existing .form-section, .form-panel definitions
2. Create unified section styling:

```css
/* Form section - grouped fields with header */
.form-section {
  background: var(--card);
  border-radius: var(--radius-lg);
  padding: 1.25rem 1.5rem;
  margin-bottom: 1.5rem;
  border: 1px solid var(--border);
}

.form-section:last-child {
  margin-bottom: 0;
}

/* Section header */
.form-section h3,
.form-section-title {
  font-size: 0.75rem;
  font-weight: 600;
  color: var(--muted-foreground);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 1rem;
  padding-bottom: 0.5rem;
  border-bottom: 1px solid var(--border);
}

/* Section divider - simple text divider */
.form-section-divider {
  font-size: 0.75rem;
  font-weight: 600;
  color: var(--muted-foreground);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin: 1.5rem 0 1rem;
  padding-bottom: 0.5rem;
  border-bottom: 1px solid var(--border);
}
```
  </action>
  <verify>Grep for "form-section {" - should have unified definition</verify>
  <done>Form sections standardized</done>
</task>

<task type="auto">
  <name>Task 3: Standardize input prefix/suffix patterns</name>
  <files>public/css/styles.css</files>
  <action>
Create consistent input addons:

1. Find existing .input-with-prefix, .input-prefix patterns
2. Create unified addon styling:

```css
/* Input with prefix (e.g., $) */
.input-with-prefix {
  position: relative;
  display: flex;
  align-items: center;
}

.input-prefix {
  position: absolute;
  left: 0.75rem;
  color: var(--muted-foreground);
  font-size: 0.875rem;
  pointer-events: none;
}

.input-with-prefix .form-control {
  padding-left: 1.75rem;
}

/* Input with suffix (e.g., %) */
.input-with-suffix {
  position: relative;
  display: flex;
  align-items: center;
}

.input-suffix {
  position: absolute;
  right: 0.75rem;
  color: var(--muted-foreground);
  font-size: 0.875rem;
  pointer-events: none;
}

.input-with-suffix .form-control {
  padding-right: 1.75rem;
}

/* Input group - prefix and suffix combined */
.input-group {
  display: flex;
  align-items: stretch;
}

.input-group-text {
  display: flex;
  align-items: center;
  padding: 0.5rem 0.75rem;
  font-size: 0.875rem;
  background: var(--muted);
  border: 1px solid var(--border);
  color: var(--muted-foreground);
}

.input-group-text:first-child {
  border-right: none;
  border-radius: var(--radius) 0 0 var(--radius);
}

.input-group-text:last-child {
  border-left: none;
  border-radius: 0 var(--radius) var(--radius) 0;
}

.input-group .form-control {
  border-radius: 0;
}

.input-group .form-control:first-child {
  border-radius: var(--radius) 0 0 var(--radius);
}

.input-group .form-control:last-child {
  border-radius: 0 var(--radius) var(--radius) 0;
}
```
  </action>
  <verify>Grep for "input-with-prefix" - should have unified styling</verify>
  <done>Input prefix/suffix patterns standardized</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] .form-row has 2-column grid
- [ ] .form-grid has flexible columns
- [ ] .form-section has card styling with header
- [ ] .input-with-prefix works correctly
- [ ] Responsive breakpoints applied
- [ ] No CSS syntax errors
</verification>

<success_criteria>
- Form layouts consistent across all modals
- Sections have uniform appearance
- Input prefixes/suffixes styled consistently
- Mobile responsive
</success_criteria>

<output>
After completion, create `.planning/phases/34-forms-validation/34-03-SUMMARY.md`
</output>
