# Plan 29-01: PO Price Warning Verification

---
phase: 29-po-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "User sees price warning when PO line item has better pricing available elsewhere"
    - "Warning shows which vendor has better price and potential savings"
    - "User can dismiss warning and continue creating/editing PO"
    - "User can view price database from warning for more details"
  artifacts:
    - path: "server/routes/purchase-orders.js"
      provides: "POST /api/purchase-orders/price-check endpoint"
      contains: "router.post('/price-check'"
    - path: "public/js/po-modals.js"
      provides: "Price warning UI in PO modal"
      contains: "checkPriceWarnings"
    - path: "public/css/styles.css"
      provides: "Price warning banner styling"
      contains: ".price-warning-banner"
  key_links:
    - from: "po-modals.js"
      to: "/api/purchase-orders/price-check"
      via: "fetch POST on save"
      pattern: "fetch.*price-check"
    - from: "purchase-orders.js price-check"
      to: "price-matcher.js"
      via: "findMasterItem and compareVendorPrices"
      pattern: "priceMatcher\\.(findMasterItem|compareVendorPrices)"
---

<objective>
Verify the existing PO price warning implementation meets PRC-05 requirements.

Purpose: Confirm price check endpoint and warning banner work end-to-end before marking v1.4 milestone complete.
Output: Verification summary documenting all PRC-05 criteria are satisfied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Discovery: Phase 29 implementation already exists:
- `server/routes/purchase-orders.js` (price-check endpoint at lines 116-175)
- `public/js/po-modals.js` (checkPriceWarnings, renderPriceWarnings, dismissPriceWarnings)
- `public/css/styles.css` (price-warning-* styles at lines 2431-2496)

This plan focuses on verification rather than implementation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Backend Price Check Endpoint</name>
  <files>server/routes/purchase-orders.js, server/price-matcher.js</files>
  <action>
    Test the price-check endpoint via curl:
    1. Get a vendor ID from the database
    2. POST to /api/purchase-orders/price-check with test items
    3. Verify response includes warnings array with expected structure
    4. Test edge cases: empty items, items with no master item match, items with matching prices
  </action>
  <verify>
    curl -X POST http://localhost:3001/api/purchase-orders/price-check \
      -H "Content-Type: application/json" \
      -d '{"vendor_id":"[test-vendor-id]","items":[{"description":"2x4x8 studs","quantity":10,"unit_price":8.50}]}'

    Response should include: warnings (array), items_checked (number), warnings_count (number)
  </verify>
  <done>Price check endpoint returns proper warning structure for items with better pricing available</done>
</task>

<task type="auto">
  <name>Task 2: Verify Frontend Warning Display</name>
  <files>public/js/po-modals.js</files>
  <action>
    Review the frontend code to verify:
    1. checkPriceWarnings() method correctly calls the API
    2. renderPriceWarnings() creates proper warning banner HTML
    3. priceWarningsContainer exists in the PO modal HTML
    4. dismissPriceWarnings() properly hides the warning
    5. Warning is triggered on PO save (before actual save)
    6. User can continue after dismissing
  </action>
  <verify>
    - checkPriceWarnings() exists and calls /api/purchase-orders/price-check
    - renderPriceWarnings() generates warning banner with item details, prices, and actions
    - priceWarningsContainer div exists in the modal HTML
    - dismissPriceWarnings() sets priceWarningsDismissed = true
    - Price check runs on savePO() before the actual POST/PATCH
  </verify>
  <done>Frontend warning system is properly wired to display, dismiss, and allow continuation</done>
</task>

<task type="auto">
  <name>Task 3: Verify CSS Styling</name>
  <files>public/css/styles.css</files>
  <action>
    Verify price warning styles exist and follow dark theme conventions:
    1. .price-warning-banner with appropriate background and border
    2. .price-warning-header with icon and dismiss button
    3. .price-warning-items with proper list styling
    4. .price-warning-actions with button styling
    5. Colors use CSS variables (not hardcoded)
  </action>
  <verify>
    grep -A 30 ".price-warning-banner" public/css/styles.css shows complete styling
    No hardcoded colors (should use var(--) syntax)
  </verify>
  <done>Price warning banner styling follows dark theme conventions</done>
</task>

<task type="auto">
  <name>Task 4: End-to-End API Test</name>
  <files></files>
  <action>
    Test complete price check flow:
    1. Get an existing master item with multiple vendor prices from the database
    2. Find a price that is significantly higher than the best price
    3. Call price-check with that higher price
    4. Verify warning is returned with correct calculations:
       - proposed_price matches input
       - best_price is lower than proposed
       - potential_savings = (proposed - best) * quantity
       - percent_higher is calculated correctly
       - better_options array includes alternative vendors
  </action>
  <verify>
    Test with a known item:
    1. Query v2_current_prices to find an item with multiple vendor prices
    2. Use the highest price + 20% as proposed price
    3. Verify warning includes all expected fields
  </verify>
  <done>Price check correctly identifies overpriced items and calculates savings</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Price check endpoint returns proper JSON structure
- [ ] Warnings include: item, proposed_price, best_price, best_vendor, potential_savings, percent_higher
- [ ] Frontend checkPriceWarnings() calls the API correctly
- [ ] Warning banner HTML is generated with all required elements
- [ ] Dismiss functionality allows user to continue with PO save
- [ ] CSS styles use dark theme variables
- [ ] No console errors when warnings are displayed
</verification>

<success_criteria>

- All verification tasks completed
- PRC-05 requirement validated: User sees price warning when creating PO with better options available
- Price check integration tested with real database data
- Warning UI renders correctly with proper styling
</success_criteria>

<output>
After completion, create `.planning/phases/29-po-integration/29-01-SUMMARY.md`
</output>
