---
phase: 32-modal-consistency
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/js/modals.js
  - public/js/po-modals.js
  - public/js/draw-modals.js
autonomous: true
must_haves:
  truths:
    - "JS-generated modals use modal-title-row class"
    - "JS-generated modals use close-btn class"
    - "Modal builders follow consistent structure"
  artifacts:
    - path: "public/js/modals.js"
      provides: "Standardized modal building in invoice modals"
    - path: "public/js/po-modals.js"
      provides: "Standardized modal building in PO modals"
    - path: "public/js/draw-modals.js"
      provides: "Standardized modal building in draw modals"
  key_links: []
---

<objective>
Update JavaScript modal builders to generate consistent markup.

Purpose: Ensure dynamically generated modals follow the same standards as static HTML.
Output: All JS-generated modal content uses standardized classes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/js/modals.js
@public/js/po-modals.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update modals.js modal generation</name>
  <files>public/js/modals.js</files>
  <action>
Find all places where modal HTML is generated dynamically and update to use standardized classes:

1. Search for `modal-header` string literals in template strings
2. Update header generation to use modal-title-row:
   - Change any `modal-title-group` to `modal-title-row`
   - Change any `modal-title` div wrapper to `modal-title-row`

3. Search for close button generation
   - Ensure all use `class="close-btn"` (not `modal-close`)

4. Search for `modal-footer` generation
   - If footer has left/right sections, use `modal-footer-left` and `modal-footer-right`

Example pattern to look for and update:
```javascript
// Before
<div class="modal-title-group">
  <h2>${title}</h2>
</div>

// After
<div class="modal-title-row">
  <h2>${title}</h2>
</div>
```
  </action>
  <verify>Grep modals.js for modal-title-row - should find usage in template strings</verify>
  <done>modals.js generates standardized modal markup</done>
</task>

<task type="auto">
  <name>Task 2: Update po-modals.js modal generation</name>
  <files>public/js/po-modals.js</files>
  <action>
Find all places where PO modal content is generated and update:

1. Search for modal header generation in renderPOModal or similar functions
   - Update to use `modal-title-row` class

2. Search for status badge placement in headers
   - Ensure badges are inside `modal-title-row` after the h2

3. Search for `modal-header-actions` generation
   - Ensure close button uses `close-btn` class

4. Update footer generation:
   - Use `modal-footer-left` for delete/destructive buttons
   - Use `modal-footer-right` for cancel/save buttons

5. If tabs are generated dynamically, ensure they follow standard pattern:
   ```javascript
   <div class="tabs">
     <button class="tab${activeTab === 'overview' ? ' active' : ''}" data-tab="overview">Overview</button>
     ...
   </div>
   ```
  </action>
  <verify>Grep po-modals.js for modal-title-row - should find usage</verify>
  <done>po-modals.js generates standardized modal markup</done>
</task>

<task type="auto">
  <name>Task 3: Update draw-modals.js if exists, or check draws.js</name>
  <files>public/js/draw-modals.js</files>
  <action>
Check for draw modal JavaScript and update:

1. First check if draw-modals.js exists, or if draw modal logic is in draws.js or another file
   - Look for functions like renderDrawModal, openDrawModal, populateDrawModal

2. Update header generation to use modal-title-row:
   ```javascript
   <div class="modal-title-row">
     <h2>Draw #${draw.draw_number}</h2>
     <span class="status-badge status-${normalizeStatusClass(draw.status)}">${draw.status}</span>
   </div>
   ```

3. Ensure tab generation follows standard pattern with visible tab buttons

4. Update footer with consistent left/right structure

5. Ensure close button uses `close-btn` class

If no separate draw-modals.js exists, check draws.js for modal generation code.
  </action>
  <verify>Search for draw modal generation code and verify modal-title-row usage</verify>
  <done>Draw modal JS generates standardized markup</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] modals.js uses modal-title-row in generated HTML
- [ ] po-modals.js uses modal-title-row in generated HTML
- [ ] Draw modal code uses modal-title-row
- [ ] All close buttons use close-btn class
- [ ] No JavaScript errors in console
</verification>

<success_criteria>
- All dynamically generated modals use standardized classes
- Modal generation follows consistent patterns
- No regressions in modal functionality
</success_criteria>

<output>
After completion, create `.planning/phases/32-modal-consistency/32-03-SUMMARY.md`
</output>
