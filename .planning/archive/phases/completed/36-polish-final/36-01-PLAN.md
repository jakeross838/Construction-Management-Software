---
phase: 36-polish-final
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/styles.css
autonomous: true
must_haves:
  truths:
    - "Interactive elements have smooth hover transitions"
    - "Button states transition smoothly"
    - "All transitions use consistent timing"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Unified transition patterns"
  key_links: []
---

<objective>
Standardize transitions and hover state animations.

Purpose: Create smooth, consistent interactive feedback across all UI elements.
Output: Unified transition timing and hover state animations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix incomplete transition declarations</name>
  <files>public/css/styles.css</files>
  <action>
Find and fix incomplete transition declarations:

1. Search for `transition: 0.3s` (missing property)
2. Replace with proper transitions:

```css
/* Fix incomplete transitions */
/* Before: transition: 0.3s; */
/* After: transition: all 0.3s ease; */
```

3. Ensure all transitions specify:
   - Property (all, opacity, transform, background, etc.)
   - Duration (0.15s, 0.2s, 0.3s)
   - Easing (ease, ease-in-out)

4. Find any `transition: all 0.2s` without easing and add `ease`
  </action>
  <verify>Grep for "transition: 0.3s;" - should find none (all should have property specified)</verify>
  <done>All transitions have complete declarations</done>
</task>

<task type="auto">
  <name>Task 2: Add transitions to button hover states</name>
  <files>public/css/styles.css</files>
  <action>
Add missing transitions to button states:

1. Find button classes without transitions: `.btn-success`, `.btn-warning`, `.btn-danger`
2. Add consistent transition to base button styles:

```css
/* Ensure all button variants have transitions */
.btn-success,
.btn-warning,
.btn-danger,
.btn-secondary,
.btn-outline {
  transition: var(--transition);
}
```

3. If `.btn` base already has transition, ensure it inherits to variants
4. Check `.btn-ghost`, `.btn-link` also have transitions

5. Verify hover states include smooth color/background changes
  </action>
  <verify>Grep for ".btn-success" and check transition property exists nearby</verify>
  <done>All button variants have smooth hover transitions</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate animation keyframes</name>
  <files>public/css/styles.css</files>
  <action>
Consolidate duplicate keyframe animations:

1. Find duplicate `@keyframes spin` definitions (reported 3 variations: 0.6s, 0.8s, 1s)
2. Create single unified spin animation:

```css
/* Unified spin animation */
@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* Use animation-duration to vary speed */
.spinner-fast { animation: spin 0.6s linear infinite; }
.spinner { animation: spin 1s linear infinite; }
.spinner-slow { animation: spin 1.5s linear infinite; }
```

3. Remove duplicate @keyframes definitions
4. Update any references to use the consolidated animation with varied duration
5. Check for duplicate `pulse` animations and consolidate similarly
  </action>
  <verify>Grep for "@keyframes spin" - should find only one definition</verify>
  <done>Keyframe animations consolidated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No `transition: 0.3s;` without property
- [ ] All button variants have transitions
- [ ] Single @keyframes spin definition
- [ ] Hover states animate smoothly
- [ ] No CSS syntax errors
</verification>

<success_criteria>
- Interactive elements respond smoothly to hover
- Animations are consistent across components
- No duplicate keyframe definitions
- Transition timing is standardized
</success_criteria>

<output>
After completion, create `.planning/phases/36-polish-final/36-01-SUMMARY.md`
</output>
