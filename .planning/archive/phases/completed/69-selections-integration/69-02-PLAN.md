---
phase: 69-selections-integration
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - public/js/selections.js
  - public/selections.html
autonomous: false

must_haves:
  truths:
    - "Selections page shows variance indicators matching catalog page"
    - "CO created from catalog appears in allowance detail view"
    - "Full workflow from catalog to CO works without errors"
  artifacts:
    - path: "public/js/selections.js"
      provides: "CO badge display in selection items"
      contains: "change_order_id"
  key_links:
    - from: "public/js/selections.js"
      to: "public/js/catalog.js"
      via: "shared data model"
      pattern: "variance"
---

<objective>
Verify end-to-end integration and add CO visibility to selections page

Purpose: Ensure the complete flow works: browse catalog -> select product -> see variance -> create CO -> view in selections page. Add visual indicator when a selection has an associated change order.

Output:
- Selections page shows CO badge on selections with linked change orders
- Full integration verified working end-to-end
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-selections-integration/69-01-PLAN.md

Key existing code:
- public/js/selections.js: renderSelectionsList() displays selection items
- v2_selections table has change_order_id column
- CO created in catalog flow needs to be visible in selections detail
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CO badge to selection items</name>
  <files>
    public/js/selections.js
    public/css/styles.css
  </files>
  <action>
    In selections.js, modify renderSelectionsList() to show CO badge:

    1. In the selection-item template, add CO indicator:
       ```javascript
       ${s.change_order_id ? `
         <span class="badge badge-warning co-badge" title="Change Order Created">
           <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
             <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
             <polyline points="14 2 14 8 20 8"></polyline>
           </svg>
           CO
         </span>
       ` : ''}
       ```

    2. Place the CO badge in the selection-item-footer next to the status badge

    3. When CO badge exists, disable the "Create Change Order" button in the overage card
       since CO already exists for this selection

    In styles.css (or selections-specific styles in selections.html):
    - .co-badge { display: inline-flex; align-items: center; gap: 4px; }
    - Make sure badge-warning colors work (orange/yellow)

    Also update the allowance detail modal's overage card:
    - Check if any selection already has a change_order_id
    - If so, show "Change Order Already Created" instead of the button
    - Or show both: info about existing CO + option to create another
  </action>
  <verify>
    1. Start server if not running
    2. Go to catalog.html, add selection that exceeds budget, create CO
    3. Go to selections.html
    4. Find the allowance that was just updated
    5. Click to open detail modal
    6. Verify the selection shows orange "CO" badge
    7. Verify the overage card reflects that CO exists
  </verify>
  <done>
    Selection items with linked change orders display a "CO" badge. Overage card shows appropriate state when CO already exists.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete integration workflow</name>
  <what-built>
    Full selections integration with:
    1. Catalog Add Selection flow with variance indicators
    2. Automatic CO prompt when exceeding budget
    3. CO creation from catalog flow
    4. CO visibility in selections detail page
  </what-built>
  <how-to-verify>
    Complete workflow test:

    1. **Setup** - Go to selections.html
       - Create a new allowance: Job=any active job, Category=Flooring, Name="Test Flooring", Budget=$1000
       - Note the allowance shows $0 selected, $0 variance

    2. **Catalog Selection** - Go to catalog.html
       - Find or add a product priced around $400/each (or any price)
       - Click to open product detail
       - Click "Add to Selection"
       - Select the job used above
       - Select "Test Flooring" allowance
       - Verify dropdown shows "Test Flooring ($1000 remaining)"
       - Verify variance indicator shows budget/remaining/projected

    3. **Within Budget** - Set qty=2 ($800 total if $400/each)
       - Verify variance indicator is GREEN
       - Verify NO CO prompt appears
       - DO NOT save yet

    4. **Exceeds Budget** - Change qty=3 ($1200 total)
       - Verify variance indicator turns RED
       - Verify CO prompt appears with overage amount ($200)
       - Check "Create change order" checkbox
       - Verify markup input appears
       - Set markup to 15%
       - Verify CO preview shows $230 (200 * 1.15)
       - Click "Save Selection"
       - Verify success toasts for both selection and CO

    5. **Verify in Selections** - Go to selections.html
       - Find "Test Flooring" allowance
       - Verify it shows: Budget $1000, Selected $1200, Variance +$200
       - Click to open detail
       - Verify selection item shows "CO" badge
       - Verify overage card shows CO was created

    6. **Verify CO in PO** (optional)
       - Go to pos.html
       - Find the PO for this job
       - Check Change Orders tab
       - Verify CO exists with ~$230 amount

    Expected: All steps complete without errors. Variance displays correctly. CO created and visible.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All Phase 69 requirements verified:
- INT-01: Selections linked to jobs via allowances (job select -> allowance select)
- INT-02: Budget tracking per category visible (variance indicator in catalog, summary in selections)
- INT-03: System generates CO when selection exceeds allowance (CO prompt + creation)
</verification>

<success_criteria>
- CO badge visible on selections with linked change orders
- Human verification confirms complete workflow works
- No console errors during full flow
- All three INT requirements demonstrably working
</success_criteria>

<output>
After completion, create `.planning/phases/69-selections-integration/69-02-SUMMARY.md`
</output>
