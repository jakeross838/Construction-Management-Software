---
phase: 11-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/styles.css
  - public/js/nav-sidebar.js
  - public/index.html
  - public/dashboard.html
autonomous: true
must_haves:
  truths:
    - "User can navigate app on mobile devices (< 768px)"
    - "User can access main navigation via hamburger menu on mobile"
    - "User can see full content without horizontal scroll on mobile"
    - "Tables are scrollable or reformatted for mobile viewing"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Mobile responsive styles"
      contains: "@media (max-width: 768px)"
    - path: "public/js/nav-sidebar.js"
      provides: "Mobile menu toggle functionality"
      contains: "toggleMobile"
  key_links:
    - from: "public/js/nav-sidebar.js"
      to: ".header"
      via: "hamburger menu toggle"
---

<objective>
Add mobile responsiveness to make the app usable on phones and tablets.

Purpose: Construction managers need access in the field on mobile devices.
Output: Responsive header, navigation, and content layouts that work on screens down to 375px.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Pattern references:
@public/css/styles.css (existing responsive patterns - many @media queries exist)
@public/js/nav-sidebar.js (existing navigation structure)
@public/index.html (main page structure with header)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile header with hamburger menu</name>
  <files>public/css/styles.css, public/js/nav-sidebar.js</files>
  <action>
Add mobile navigation with hamburger toggle:

1. **In public/css/styles.css**, add at the end:
```css
/* Mobile Navigation */
.mobile-menu-btn {
  display: none;
  background: none;
  border: none;
  color: var(--foreground);
  font-size: 1.5rem;
  cursor: pointer;
  padding: 0.5rem;
}

@media (max-width: 768px) {
  .mobile-menu-btn {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .header-top {
    padding: 0 1rem;
  }

  .main-nav {
    display: none;
    position: absolute;
    top: 56px;
    left: 0;
    right: 0;
    background: var(--card);
    border-bottom: 1px solid var(--border);
    flex-direction: column;
    padding: 0.5rem;
    gap: 0.25rem;
    z-index: 99;
  }

  .main-nav.mobile-open {
    display: flex;
  }

  .main-nav-link {
    width: 100%;
    padding: 0.75rem 1rem;
  }

  .header-sub {
    display: none;
  }

  .header-sub.mobile-open {
    display: flex;
    position: absolute;
    top: 56px;
    left: 0;
    right: 0;
    background: var(--card-elevated);
    flex-wrap: wrap;
    padding: 0.5rem;
    z-index: 98;
    border-bottom: 1px solid var(--border);
  }

  /* Push main content down when menu is open */
  .main-nav.mobile-open ~ .header-sub,
  body.mobile-menu-open .header-sub {
    top: auto;
    margin-top: 0;
  }

  .sub-nav {
    flex-wrap: wrap;
    gap: 0.25rem;
  }

  .sub-nav-link {
    flex: 0 0 auto;
  }
}
```

2. **In public/js/nav-sidebar.js**, update the export API around line 152:
```javascript
// Export API
window.NavSidebar = {
  init,
  toggle: () => {},
  toggleMobile: () => {
    const mainNav = document.querySelector('.main-nav');
    const headerSub = document.querySelector('.header-sub');
    const isOpen = mainNav?.classList.contains('mobile-open');

    if (mainNav) {
      mainNav.classList.toggle('mobile-open', !isOpen);
    }
    if (headerSub) {
      headerSub.classList.toggle('mobile-open', !isOpen);
    }
    document.body.classList.toggle('mobile-menu-open', !isOpen);
  },
  closeMobile: () => {
    document.querySelector('.main-nav')?.classList.remove('mobile-open');
    document.querySelector('.header-sub')?.classList.remove('mobile-open');
    document.body.classList.remove('mobile-menu-open');
  },
  isCollapsed: () => false
};
```

3. **Update init function** to add hamburger button to header after the brand:
In the init() function, after checking for mainNav, add:
```javascript
// Add mobile hamburger if not exists
if (!document.querySelector('.mobile-menu-btn')) {
  const headerTop = document.querySelector('.header-top');
  const brand = headerTop?.querySelector('.header-brand');
  if (brand && headerTop) {
    const hamburger = document.createElement('button');
    hamburger.className = 'mobile-menu-btn';
    hamburger.innerHTML = '☰';
    hamburger.setAttribute('aria-label', 'Toggle menu');
    hamburger.onclick = window.NavSidebar.toggleMobile;
    headerTop.insertBefore(hamburger, brand.nextSibling);
  }
}
```
  </action>
  <verify>Open app on mobile viewport (375px), verify hamburger appears and toggles navigation</verify>
  <done>Mobile hamburger menu opens and closes navigation on small screens</done>
</task>

<task type="auto">
  <name>Task 2: Make data tables mobile-friendly</name>
  <files>public/css/styles.css</files>
  <action>
Add responsive table styles to handle wide data tables on mobile:

```css
/* Mobile-friendly tables */
@media (max-width: 768px) {
  .data-table-container,
  .table-container {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    margin: 0 -1rem;
    padding: 0 1rem;
  }

  .data-table,
  table {
    min-width: 600px;
  }

  .data-table th,
  .data-table td {
    padding: 0.5rem 0.75rem;
    font-size: 0.8125rem;
  }

  /* Stack filter controls on mobile */
  .filter-bar,
  .filters,
  .page-header-bar {
    flex-direction: column;
    gap: 0.75rem;
    align-items: stretch;
  }

  .filter-bar select,
  .filter-bar input,
  .filters select,
  .filters input {
    width: 100%;
  }

  /* Adjust main content padding */
  .main {
    padding: 1rem;
  }

  .app-body {
    padding: 0;
  }

  /* Stats grid stacks on mobile */
  .dashboard-stats,
  .stats-row,
  .stat-cards {
    grid-template-columns: 1fr;
  }

  /* Card adjustments */
  .card,
  .stat-card,
  .dashboard-section {
    padding: 1rem;
    border-radius: var(--radius);
  }

  /* Button group stacking */
  .btn-group,
  .modal-footer-right {
    flex-direction: column;
    gap: 0.5rem;
  }

  .btn-group .btn,
  .modal-footer-right .btn {
    width: 100%;
  }
}

/* Even smaller screens */
@media (max-width: 480px) {
  .header-top {
    padding: 0 0.75rem;
    height: 48px;
  }

  .brand-name {
    font-size: 0.875rem;
  }

  .main {
    padding: 0.75rem;
  }

  h1 {
    font-size: 1.25rem;
  }

  h2 {
    font-size: 1rem;
  }
}
```
  </action>
  <verify>Open invoices page on 375px viewport, verify table is horizontally scrollable</verify>
  <done>Tables scroll horizontally on mobile, filters stack vertically</done>
</task>

<task type="auto">
  <name>Task 3: Mobile-optimize modals</name>
  <files>public/css/styles.css</files>
  <action>
Add responsive modal styles for mobile devices:

```css
/* Mobile modal optimization */
@media (max-width: 768px) {
  .modal-fullscreen,
  .modal-fullscreen-dark {
    width: 100vw !important;
    height: 100vh !important;
    max-width: none !important;
    max-height: none !important;
    border-radius: 0 !important;
    margin: 0 !important;
  }

  .modal-content {
    border-radius: 0;
  }

  .modal-header {
    padding: 0.75rem 1rem;
  }

  .modal-header h2 {
    font-size: 1rem;
  }

  .modal-body {
    padding: 1rem;
  }

  .modal-footer {
    padding: 0.75rem 1rem;
    flex-direction: column;
    gap: 0.75rem;
  }

  .modal-footer-left,
  .modal-footer-right {
    width: 100%;
    text-align: center;
  }

  /* Form grid single column on mobile */
  .form-grid {
    grid-template-columns: 1fr !important;
  }

  /* Tabs wrap on mobile */
  .tabs {
    flex-wrap: wrap;
    gap: 0.25rem;
  }

  .tab {
    flex: 1 1 auto;
    min-width: 80px;
    padding: 0.5rem 0.75rem;
    font-size: 0.75rem;
  }

  /* PDF panel in split view */
  .modal-split-view {
    grid-template-columns: 1fr !important;
    grid-template-rows: 200px 1fr !important;
  }

  .pdf-panel {
    max-height: 200px;
    overflow: hidden;
  }
}
```
  </action>
  <verify>Open invoice modal on 375px viewport, verify it fills screen and is scrollable</verify>
  <done>Modals take full screen on mobile with proper padding and scrolling</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Hamburger menu appears on screens < 768px
- [ ] Clicking hamburger toggles navigation visibility
- [ ] Tables are horizontally scrollable on mobile
- [ ] Modals fill the screen on mobile devices
- [ ] No horizontal scroll on body at 375px viewport
- [ ] All buttons and inputs are tap-friendly (min 44px)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- App is usable on 375px mobile viewport
- No broken layouts or overlapping content
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-polish/11-01-SUMMARY.md`
</output>
