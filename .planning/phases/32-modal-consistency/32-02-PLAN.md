---
phase: 32-modal-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - public/pos.html
  - public/draws.html
autonomous: true
must_haves:
  truths:
    - "All HTML modals use modal-title-row class for headers"
    - "All fullscreen modals use modal-fullscreen-dark class"
    - "Modal footer buttons follow left/right structure"
  artifacts:
    - path: "public/index.html"
      provides: "Standardized invoice modal structure"
    - path: "public/pos.html"
      provides: "Standardized PO modal structure"
    - path: "public/draws.html"
      provides: "Standardized draw modal structure"
  key_links: []
---

<objective>
Update HTML modal structures to use standardized classes.

Purpose: Ensure all static HTML modals follow the UI-STANDARDS modal pattern.
Output: Consistent modal markup across main HTML files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/index.html
@public/pos.html
@public/draws.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize index.html modal structures</name>
  <files>public/index.html</files>
  <action>
Update modal structures in index.html:

1. Find the invoice detail modal (editModal or similar)
   - Ensure it uses `class="modal modal-fullscreen-dark"` if it's a large split-view modal
   - Update header to use `.modal-title-row` pattern:
     ```html
     <div class="modal-header">
       <div class="modal-title-row">
         <h2>Invoice Details</h2>
         <span class="status-badge" id="statusBadge"></span>
       </div>
       <div class="modal-header-actions">
         <button class="close-btn" onclick="closeModal()">&times;</button>
       </div>
     </div>
     ```

2. Find simple modals (denial, payment, etc.)
   - Keep `class="modal"` with appropriate size class
   - Ensure footer uses standard structure

3. Standardize all modal footers:
   - If has destructive action: use `modal-footer-left` and `modal-footer-right`
   - If simple confirm/cancel: direct buttons in `modal-footer`
  </action>
  <verify>Grep index.html for modal-title-row - should find usage</verify>
  <done>index.html modals use standardized structure</done>
</task>

<task type="auto">
  <name>Task 2: Standardize pos.html modal structures</name>
  <files>public/pos.html</files>
  <action>
Update modal structures in pos.html:

1. Find the PO detail modal
   - Should use `class="modal modal-fullscreen-dark"`
   - Update header to consistent pattern:
     ```html
     <div class="modal-header">
       <div class="modal-title-row">
         <h2 id="poModalTitle">Purchase Order</h2>
         <span class="status-badge" id="poStatusBadge"></span>
       </div>
       <div class="modal-header-actions">
         <!-- Action buttons -->
         <button class="close-btn" onclick="closePOModal()">&times;</button>
       </div>
     </div>
     ```

2. Update any create/edit modals to use consistent structure

3. Ensure close button uses `.close-btn` class (not modal-close)
  </action>
  <verify>Grep pos.html for modal-title-row - should find usage</verify>
  <done>pos.html modals use standardized structure</done>
</task>

<task type="auto">
  <name>Task 3: Standardize draws.html modal structures</name>
  <files>public/draws.html</files>
  <action>
Update modal structures in draws.html:

1. Find the draw detail modal
   - Should use `class="modal modal-fullscreen-dark"`
   - Update header structure:
     ```html
     <div class="modal-header">
       <div class="modal-title-row">
         <h2 id="drawModalTitle">Draw Details</h2>
         <span class="status-badge" id="drawStatusBadge"></span>
       </div>
       <div class="modal-header-actions">
         <button class="btn btn-secondary" id="exportBtn">Export</button>
         <button class="btn btn-secondary" id="printBtn">Print</button>
         <button class="close-btn" onclick="closeDrawModal()">&times;</button>
       </div>
     </div>
     ```

2. If modal has tabs, ensure tab structure follows standard:
   ```html
   <div class="tabs">
     <button class="tab active" data-tab="summary">Summary</button>
     <button class="tab" data-tab="g702">G702</button>
     <button class="tab" data-tab="g703">G703</button>
     <button class="tab" data-tab="invoices">Invoices</button>
   </div>
   ```

3. Update footer to consistent structure with left/right sections if needed
  </action>
  <verify>Grep draws.html for modal-title-row - should find usage</verify>
  <done>draws.html modals use standardized structure</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All modals in index.html use modal-title-row
- [ ] All modals in pos.html use modal-title-row
- [ ] All modals in draws.html use modal-title-row
- [ ] Close buttons all use .close-btn class
- [ ] No broken modal layouts (test in browser)
</verification>

<success_criteria>
- All HTML modals follow UI-STANDARDS modal pattern
- Headers use consistent modal-title-row structure
- Close buttons are positioned consistently
- Footer button placement is standardized
</success_criteria>

<output>
After completion, create `.planning/phases/32-modal-consistency/32-02-SUMMARY.md`
</output>
