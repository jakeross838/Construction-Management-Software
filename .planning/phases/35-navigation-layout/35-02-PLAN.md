---
phase: 35-navigation-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/styles.css
autonomous: true
must_haves:
  truths:
    - "Page headers have consistent styling"
    - "Content area spacing is uniform"
    - "Responsive breakpoints are consolidated"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Unified page header and content spacing CSS"
  key_links: []
---

<objective>
Standardize page headers, content area spacing, and consolidate responsive breakpoints.

Purpose: Create consistent page structure and responsive behavior.
Output: Unified CSS for page headers, content spacing, and responsive patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize page header patterns</name>
  <files>public/css/styles.css</files>
  <action>
Create unified page header styling:

1. Find existing page header classes: `.dashboard-header`, `.budget-page-header`, `.page-header-bar`
2. Create unified page header pattern:

```css
/* Page header - standard pattern */
.page-header {
  margin-bottom: 1.5rem;
}

.page-header h1,
.page-title {
  font-size: 1.5rem;
  font-weight: 600;
  color: var(--foreground);
  margin: 0;
}

.page-subtitle {
  font-size: 0.875rem;
  color: var(--muted-foreground);
  margin-top: 0.25rem;
}

/* Page header with stats bar */
.page-header-stats {
  display: flex;
  gap: 1rem;
  margin-top: 1rem;
  flex-wrap: wrap;
}

/* Page header with actions */
.page-header-actions {
  display: flex;
  gap: 0.5rem;
  align-items: center;
}

/* Page header row - title + actions */
.page-header-row {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 1rem;
}

@media (max-width: 768px) {
  .page-header-row {
    flex-direction: column;
    align-items: stretch;
  }

  .page-header-actions {
    justify-content: flex-start;
  }
}
```

3. Alias existing classes to new pattern where possible
  </action>
  <verify>Grep for "page-header {" - should have unified definition</verify>
  <done>Page header patterns standardized</done>
</task>

<task type="auto">
  <name>Task 2: Standardize content area spacing</name>
  <files>public/css/styles.css</files>
  <action>
Ensure consistent content area spacing:

1. Find existing `.main` and content container definitions
2. Verify consistent padding values are applied:

```css
/* Main content area - standard spacing */
.main {
  flex: 1;
  padding: 1.5rem;
  max-width: 1400px;
  margin: 0 auto;
  width: 100%;
}

/* Content sections */
.content-section {
  margin-bottom: 1.5rem;
}

.content-section:last-child {
  margin-bottom: 0;
}

/* Responsive content spacing */
@media (max-width: 768px) {
  .main {
    padding: 1rem;
  }

  .content-section {
    margin-bottom: 1rem;
  }
}

@media (max-width: 480px) {
  .main {
    padding: 0.75rem;
  }
}
```

3. Ensure `.form-section`, `.card`, and other containers have consistent margin/padding
4. Add mobile overrides where missing
  </action>
  <verify>Grep for "@media.*480px" with ".main" - should have padding override</verify>
  <done>Content area spacing standardized</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate responsive breakpoints</name>
  <files>public/css/styles.css</files>
  <action>
Consolidate and document responsive breakpoints:

1. Find all navigation/layout related media queries
2. Create consolidated responsive section for layout:

```css
/* ================================================================
   RESPONSIVE LAYOUT BREAKPOINTS
   Primary: 768px (tablet/mobile)
   Secondary: 480px (small mobile)
   Large: 1024px (fullscreen modals)
   ================================================================ */

/* Tablet breakpoint - primary layout changes */
@media (max-width: 768px) {
  /* Header adjustments */
  .header-top {
    padding: 0 1rem;
  }

  /* Navigation mobile mode */
  .mobile-menu-btn {
    display: flex;
  }

  .main-nav {
    display: none;
    flex-direction: column;
  }

  .main-nav.mobile-open {
    display: flex;
  }

  /* Content spacing */
  .main {
    padding: 1rem;
  }

  /* Page headers stack on mobile */
  .page-header-row {
    flex-direction: column;
  }
}

/* Small mobile breakpoint */
@media (max-width: 480px) {
  .header-top {
    height: 48px;
    padding: 0 0.75rem;
  }

  .main {
    padding: 0.75rem;
  }

  .page-header h1,
  .page-title {
    font-size: 1.25rem;
  }
}
```

3. Remove duplicate media query definitions for same breakpoint
4. Add clear section comment for responsive rules
  </action>
  <verify>Check that responsive section is clearly documented with comment header</verify>
  <done>Responsive breakpoints consolidated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] .page-header has unified definition
- [ ] .main has consistent padding at all breakpoints
- [ ] Responsive breakpoints are documented
- [ ] Mobile layout works correctly
- [ ] No CSS syntax errors
</verification>

<success_criteria>
- Page headers consistent across all pages
- Content spacing uniform at all breakpoints
- Responsive behavior predictable
- Layout works on mobile/tablet/desktop
</success_criteria>

<output>
After completion, create `.planning/phases/35-navigation-layout/35-02-SUMMARY.md`
</output>
