---
phase: 31-component-uniformity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/js/co-app.js
  - public/js/job-profile.js
  - public/js/inspections.js
  - public/js/price-intelligence.js
autonomous: true
must_haves:
  truths:
    - "Change order status badges use normalized class names"
    - "Job profile status badges use normalized class names"
    - "Inspection status badges use normalized class names"
    - "Price intelligence source badges use normalized class names"
  artifacts:
    - path: "public/js/co-app.js"
      provides: "Normalized CO status class generation"
    - path: "public/js/job-profile.js"
      provides: "Normalized job status class generation"
    - path: "public/js/inspections.js"
      provides: "Normalized inspection status class generation"
    - path: "public/js/price-intelligence.js"
      provides: "Normalized price source class generation"
  key_links: []
---

<objective>
Update remaining JS modules to use normalized status classes.

Purpose: Complete status class normalization across all JavaScript files.
Output: Consistent hyphen-based status classes in CO, Job, Inspection, and Price Intelligence modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/js/co-app.js
@public/js/job-profile.js
@public/js/inspections.js
@public/js/price-intelligence.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update co-app.js status classes</name>
  <files>public/js/co-app.js</files>
  <action>
Add normalizeStatusClass helper at top of file:

```javascript
const normalizeStatusClass = window.normalizeStatusClass || function(status) {
  if (!status) return '';
  return status.toString().toLowerCase().replace(/_/g, '-');
};
```

Update status badge rendering:
- Line ~595: `status-badge status-${co.status}` -> `status-badge status-${normalizeStatusClass(co.status)}`
  </action>
  <verify>Search co-app.js for normalizeStatusClass - should be used</verify>
  <done>co-app.js uses normalized status classes</done>
</task>

<task type="auto">
  <name>Task 2: Update job-profile.js status classes</name>
  <files>public/js/job-profile.js</files>
  <action>
Add normalizeStatusClass helper at top of file:

```javascript
const normalizeStatusClass = window.normalizeStatusClass || function(status) {
  if (!status) return '';
  return status.toString().toLowerCase().replace(/_/g, '-');
};
```

Update status badge rendering:
- Line ~203: `status-badge status-${job.status || 'active'}` -> `status-badge status-${normalizeStatusClass(job.status || 'active')}`
  </action>
  <verify>Search job-profile.js for normalizeStatusClass - should be used</verify>
  <done>job-profile.js uses normalized status classes</done>
</task>

<task type="auto">
  <name>Task 3: Update inspections.js status classes</name>
  <files>public/js/inspections.js</files>
  <action>
Add normalizeStatusClass helper at top of file:

```javascript
const normalizeStatusClass = window.normalizeStatusClass || function(status) {
  if (!status) return '';
  return status.toString().toLowerCase().replace(/_/g, '-');
};
```

Update status badge rendering:
- Line ~531: `status-badge status-${getStatusClass(inspection.result)}` -> `status-badge status-${normalizeStatusClass(getStatusClass(inspection.result))}`

Note: The getStatusClass function already returns a status string - we just need to normalize it.
  </action>
  <verify>Search inspections.js for normalizeStatusClass - should be used</verify>
  <done>inspections.js uses normalized status classes</done>
</task>

<task type="auto">
  <name>Task 4: Update price-intelligence.js status classes</name>
  <files>public/js/price-intelligence.js</files>
  <action>
Add normalizeStatusClass helper at top of file:

```javascript
const normalizeStatusClass = window.normalizeStatusClass || function(status) {
  if (!status) return '';
  return status.toString().toLowerCase().replace(/_/g, '-');
};
```

Update source badge rendering:
- Line ~468-469: `status-badge status-${sourceClass}` -> `status-badge status-${normalizeStatusClass(sourceClass)}`

These are for source type badges (invoice, quote, manual) - ensure they follow the same pattern.
  </action>
  <verify>Search price-intelligence.js for normalizeStatusClass - should be used</verify>
  <done>price-intelligence.js uses normalized status classes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] co-app.js has normalizeStatusClass and uses it
- [ ] job-profile.js has normalizeStatusClass and uses it
- [ ] inspections.js has normalizeStatusClass and uses it
- [ ] price-intelligence.js has normalizeStatusClass and uses it
- [ ] No JavaScript errors in browser console
</verification>

<success_criteria>
- All remaining JS files use normalized status classes
- Consistent with changes from Plans 31-01 and 31-02
- Complete coverage of status class normalization
</success_criteria>

<output>
After completion, create `.planning/phases/31-component-uniformity/31-03-SUMMARY.md`
</output>
