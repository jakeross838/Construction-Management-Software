---
phase: 46-ai-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/ai-processor.js
autonomous: true

must_haves:
  truths:
    - "Job matching requires 70%+ confidence to auto-assign"
    - "Threshold raised from 0.50/0.60 to 0.70"
    - "Low confidence matches show job picker instead of auto-assign"
  artifacts:
    - path: "server/ai-processor.js"
      provides: "Raised confidence thresholds for job matching"
  key_links:
    - from: "CONFIDENCE_THRESHOLDS"
      to: "findMatchingJob/scanTextForKnownJobs"
      via: "threshold check"
      pattern: "0\\.7|THRESHOLDS\\.LOW"
---

<objective>
Raise job matching confidence threshold from 0.5 to 0.7 minimum.

Purpose: AI-INT-01 - Reduce false positive job matches by requiring higher confidence.

Output: Job matches only auto-assigned when confidence >= 70%.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update CONFIDENCE_THRESHOLDS</name>
  <files>server/ai-processor.js</files>
  <action>
Update the CONFIDENCE_THRESHOLDS.LOW value from 0.60 to 0.70:

Find around line 50:
```javascript
LOW: 0.60      // Don't auto-assign, show picker
```

Change to:
```javascript
LOW: 0.70      // Don't auto-assign, show picker (raised from 0.50)
```

This affects findMatchingJob which uses `CONFIDENCE_THRESHOLDS.LOW`.
  </action>
  <verify>Grep for CONFIDENCE_THRESHOLDS.LOW in ai-processor.js</verify>
  <done>CONFIDENCE_THRESHOLDS.LOW raised to 0.70</done>
</task>

<task type="auto">
  <name>Task 2: Update scanTextForKnownJobs threshold</name>
  <files>server/ai-processor.js</files>
  <action>
Find the scanTextForKnownJobs function (around line 765):
```javascript
if (bestMatch && bestConfidence > 0.5) {
```

Change to:
```javascript
if (bestMatch && bestConfidence >= 0.70) {  // AI-INT-01: Raised from 0.5
```

This ensures text scanning only returns matches with 70%+ confidence.
  </action>
  <verify>Grep for "bestConfidence" in scanTextForKnownJobs</verify>
  <done>Text scanning threshold raised to 0.70</done>
</task>

</tasks>

<success_criteria>
- CONFIDENCE_THRESHOLDS.LOW is 0.70
- scanTextForKnownJobs requires >= 0.70 confidence
- findMatchingJob uses the new threshold via CONFIDENCE_THRESHOLDS.LOW
- Low confidence matches result in null job (user picks manually)
</success_criteria>
