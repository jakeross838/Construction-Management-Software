---
phase: 33-tables-lists
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - public/budgets.html
  - public/reconciliation.html
  - public/price-intelligence.html
autonomous: true
must_haves:
  truths:
    - "budgets.html uses standardized table classes"
    - "reconciliation.html uses standardized table classes"
    - "price-intelligence.html uses standardized table classes"
  artifacts:
    - path: "public/budgets.html"
      provides: "Updated table markup"
    - path: "public/reconciliation.html"
      provides: "Updated table markup"
    - path: "public/price-intelligence.html"
      provides: "Updated table markup"
  key_links: []
---

<objective>
Update HTML pages to use standardized table classes and patterns.

Purpose: Apply CSS standards to HTML table markup.
Output: Consistent table markup in budgets, reconciliation, and price-intelligence pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/budgets.html
@public/reconciliation.html
@public/price-intelligence.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update budgets.html table patterns</name>
  <files>public/budgets.html</files>
  <action>
Update budgets.html to use standardized patterns:

1. Find any non-standard table classes and add `data-table` alongside for consistency
   - Keep `g703-table` for financial tables (specialized)
   - Look for any `invoices-table` and ensure it has proper styling

2. Standardize empty state markup:
   - Find any inline empty states
   - Update to use standard structure:
   ```html
   <tr class="empty-row">
     <td colspan="X" class="empty-state">
       <div class="empty-state-icon">📋</div>
       <div class="empty-state-title">No items found</div>
       <div class="empty-state-message">Description text</div>
     </td>
   </tr>
   ```

3. Update action columns to use `.col-actions` class

4. Ensure toolbar uses `.toolbar` or `.data-toolbar` class
  </action>
  <verify>Grep budgets.html for "col-actions" - should find standardized usage</verify>
  <done>budgets.html uses standardized table patterns</done>
</task>

<task type="auto">
  <name>Task 2: Update reconciliation.html table patterns</name>
  <files>public/reconciliation.html</files>
  <action>
Update reconciliation.html to use standardized patterns:

1. Find `.history-table` and ensure it inherits from data-table styling
   - The CSS alias in 33-01 will handle styling
   - Optionally add `data-table` class alongside for explicit inheritance

2. Update any inline empty states to standard structure

3. Ensure action columns use `.col-actions`

4. Standardize toolbar class:
   - Find toolbar/filter section
   - Use `.toolbar` with `.toolbar-left` and `.toolbar-right`

5. Update filter dropdowns to use `.filter-select` or be within `.filter-group`
  </action>
  <verify>Grep reconciliation.html for "toolbar" - should find standardized class</verify>
  <done>reconciliation.html uses standardized table patterns</done>
</task>

<task type="auto">
  <name>Task 3: Update price-intelligence.html table patterns</name>
  <files>public/price-intelligence.html</files>
  <action>
Update price-intelligence.html to use standardized patterns:

1. Find any table elements and ensure they use `data-table` class

2. Standardize any `.invoices-table` usage:
   - Add `data-table data-table-hover` classes
   - Keep invoices-table for backward compatibility

3. Update empty states to standard structure:
   ```html
   <div class="empty-state">
     <div class="empty-state-icon">📦</div>
     <div class="empty-state-title">No price data</div>
     <div class="empty-state-message">Add items to track pricing</div>
   </div>
   ```

4. Ensure any toolbar/filter sections use standard classes

5. Verify action buttons in tables use `.btn-ghost .btn-sm` pattern in `.col-actions`
  </action>
  <verify>Grep price-intelligence.html for "data-table" - should find usage</verify>
  <done>price-intelligence.html uses standardized table patterns</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] budgets.html tables use standard classes
- [ ] reconciliation.html tables use standard classes
- [ ] price-intelligence.html tables use standard classes
- [ ] Empty states follow standard structure
- [ ] Action columns use .col-actions
- [ ] Toolbars use .toolbar class
- [ ] No broken table layouts
</verification>

<success_criteria>
- All three pages use standardized table markup
- Empty states consistent
- Action columns standardized
- Toolbars uniform
</success_criteria>

<output>
After completion, create `.planning/phases/33-tables-lists/33-03-SUMMARY.md`
</output>
