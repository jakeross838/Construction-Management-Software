# Phase 112-05: Apply Style Guide & Add Searchable Dropdowns

## Objective
Fix estimates page styling to match STYLE_GUIDE.md and add searchable dropdowns for better UX.

## Context
@STYLE_GUIDE.md - Application design standards
@public/estimates-budget.html - Current page (inconsistent styling)
@public/js/estimates-budget.js - JavaScript
@public/index.html - Reference for correct styling

Estimates page uses old styles (light theme colors, wrong CSS variables). Need to:
1. Apply SearchablePicker component for all dropdowns
2. Fix all colors/spacing/fonts to match style guide
3. Make page visually consistent with rest of app

## Tasks

### Task 1: Create SearchablePicker Component

Add to `public/js/estimates-budget.js`:

```javascript
// SearchablePicker Component (from STYLE_GUIDE.md)
class SearchablePicker {
  constructor(containerId, options = {}) {
    this.container = document.getElementById(containerId);
    this.options = options;
    this.items = [];
    this.selectedValue = null;
    this.isOpen = false;

    this.init();
  }

  init() {
    const html = `
      <div class="search-picker">
        <input type="text"
               class="search-picker-input"
               placeholder="${this.options.placeholder || 'Search...'}"
               autocomplete="off">
        <button class="search-picker-clear" style="display: none;">&times;</button>
        <div class="search-picker-dropdown" style="display: none;"></div>
        <input type="hidden" class="search-picker-value">
      </div>
    `;

    this.container.innerHTML = html;

    this.input = this.container.querySelector('.search-picker-input');
    this.clearBtn = this.container.querySelector('.search-picker-clear');
    this.dropdown = this.container.querySelector('.search-picker-dropdown');
    this.hiddenInput = this.container.querySelector('.search-picker-value');

    this.attachEvents();
  }

  attachEvents() {
    this.input.addEventListener('focus', () => this.open());
    this.input.addEventListener('input', (e) => this.filter(e.target.value));
    this.clearBtn.addEventListener('click', () => this.clear());

    document.addEventListener('click', (e) => {
      if (!this.container.contains(e.target)) {
        this.close();
      }
    });
  }

  setItems(items) {
    this.items = items;
    this.render();
  }

  render(filteredItems = this.items) {
    if (filteredItems.length === 0) {
      this.dropdown.innerHTML = '<div class="search-picker-empty">No results</div>';
      return;
    }

    const html = filteredItems.map(item => {
      const isSelected = item.value === this.selectedValue;
      return `
        <div class="search-picker-item ${isSelected ? 'selected' : ''}"
             data-value="${item.value}">
          ${this.formatItem(item)}
        </div>
      `;
    }).join('');

    this.dropdown.innerHTML = html;

    // Attach click handlers
    this.dropdown.querySelectorAll('.search-picker-item').forEach(el => {
      el.addEventListener('click', () => {
        const value = el.dataset.value;
        const item = this.items.find(i => i.value === value);
        this.select(item);
      });
    });
  }

  formatItem(item) {
    if (item.code) {
      // For cost codes: "06100 - Rough Carpentry"
      return `
        <span class="picker-code">${item.code}</span>
        <span class="picker-name">${item.label}</span>
      `;
    }
    return item.label;
  }

  filter(query) {
    const filtered = this.items.filter(item => {
      const searchText = `${item.label} ${item.code || ''}`.toLowerCase();
      return searchText.includes(query.toLowerCase());
    });
    this.render(filtered);
  }

  open() {
    this.isOpen = true;
    this.dropdown.style.display = 'block';
    this.render();
  }

  close() {
    this.isOpen = false;
    this.dropdown.style.display = 'none';
  }

  select(item) {
    this.selectedValue = item.value;
    this.input.value = item.label;
    this.hiddenInput.value = item.value;
    this.clearBtn.style.display = 'block';
    this.close();

    if (this.options.onChange) {
      this.options.onChange(item.value, item);
    }
  }

  clear() {
    this.selectedValue = null;
    this.input.value = '';
    this.hiddenInput.value = '';
    this.clearBtn.style.display = 'none';

    if (this.options.onChange) {
      this.options.onChange(null);
    }
  }

  getValue() {
    return this.selectedValue;
  }

  setValue(value) {
    const item = this.items.find(i => i.value === value);
    if (item) {
      this.select(item);
    }
  }
}
```

Add CSS to `public/estimates-budget.html` (from STYLE_GUIDE.md):

```css
/* SearchablePicker Component */
.search-picker {
  position: relative;
  width: 100%;
}

.search-picker-input {
  width: 100%;
  padding: 0.5rem 2.5rem 0.5rem 0.75rem;
  font-size: 0.875rem;
  background: var(--card);
  color: var(--foreground);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  transition: border-color 0.15s ease, box-shadow 0.15s ease;
}

.search-picker-input:hover {
  border-color: var(--border-light);
}

.search-picker-input:focus {
  outline: none;
  border-color: var(--ring);
  box-shadow: 0 0 0 2px rgba(247, 247, 247, 0.1);
}

.search-picker-clear {
  position: absolute;
  right: 0.5rem;
  top: 50%;
  transform: translateY(-50%);
  padding: 0.25rem;
  background: none;
  border: none;
  color: var(--text-secondary);
  cursor: pointer;
  font-size: 1.25rem;
  line-height: 1;
}

.search-picker-clear:hover {
  color: var(--foreground);
}

.search-picker-dropdown {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  margin-top: 0.25rem;
  max-height: 300px;
  overflow-y: auto;
  background: var(--card-elevated);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
  z-index: 1000;
}

.search-picker-item {
  padding: 0.5rem 0.75rem;
  cursor: pointer;
  transition: background 0.1s ease;
  display: flex;
  gap: 0.5rem;
}

.search-picker-item:hover,
.search-picker-item.selected {
  background: var(--card);
}

.picker-code {
  font-family: monospace;
  min-width: 60px;
  color: var(--foreground);
}

.picker-name {
  color: var(--text-secondary);
}

.search-picker-empty {
  padding: 1rem;
  text-align: center;
  color: var(--text-secondary);
  font-size: 0.875rem;
}
```

### Task 2: Update Modals to Use SearchablePicker

Replace standard `<select>` elements with `<div>` containers for pickers.

**Phase Modal:**
```html
<div class="modal-body">
  <div class="form-group">
    <label class="form-label">Phase Name <span class="required">*</span></label>
    <input type="text" id="phaseName" class="form-input">
  </div>
  <div class="form-group">
    <label class="form-label">Phase Code</label>
    <input type="text" id="phaseCode" class="form-input">
  </div>
  <div class="form-group">
    <label class="form-label">Description</label>
    <textarea id="phaseDescription" class="form-input" rows="3"></textarea>
  </div>
</div>
```

**Group Modal:**
```html
<div class="modal-body">
  <div class="form-group">
    <label class="form-label">Phase <span class="required">*</span></label>
    <div id="groupPhasePicker"></div>
  </div>
  <div class="form-group">
    <label class="form-label">Group Name <span class="required">*</span></label>
    <input type="text" id="groupName" class="form-input">
  </div>
  <div class="form-group">
    <label class="form-label">Description</label>
    <textarea id="groupDescription" class="form-input" rows="3"></textarea>
  </div>
</div>
```

**Subgroup Modal:**
```html
<div class="modal-body">
  <div class="form-group">
    <label class="form-label">Group <span class="required">*</span></label>
    <div id="subgroupGroupPicker"></div>
  </div>
  <div class="form-group">
    <label class="form-label">Subgroup Name <span class="required">*</span></label>
    <input type="text" id="subgroupName" class="form-input">
  </div>
  <div class="form-group">
    <label class="form-label">Description</label>
    <textarea id="subgroupDescription" class="form-input" rows="3"></textarea>
  </div>
</div>
```

**Line Item Modal - Subgroup Picker:**
```html
<div class="form-group">
  <label class="form-label">Subgroup <span class="required">*</span></label>
  <div id="lineSubgroupPicker"></div>
</div>

<div class="form-group">
  <label class="form-label">Cost Code</label>
  <div id="lineCostCodePicker"></div>
</div>
```

Update JavaScript to initialize pickers:

```javascript
let groupPhasePicker;
let subgroupGroupPicker;
let lineSubgroupPicker;
let lineCostCodePicker;

function openGroupModal() {
  if (!currentEstimate) {
    showToast('No estimate loaded', 'error');
    return;
  }

  // Initialize picker if not already
  if (!groupPhasePicker) {
    groupPhasePicker = new SearchablePicker('groupPhasePicker', {
      placeholder: 'Search phases...'
    });
  }

  // Populate phases
  const phases = (currentEstimate.phases || []).map(p => ({
    value: p.id,
    label: p.name
  }));
  groupPhasePicker.setItems(phases);

  document.getElementById('groupName').value = '';
  document.getElementById('groupDescription').value = '';

  const modal = document.getElementById('groupModal');
  modal.style.display = 'flex';
  setTimeout(() => modal.classList.add('show'), 10);
}

async function saveGroup() {
  const phaseId = groupPhasePicker.getValue();
  const name = document.getElementById('groupName').value.trim();

  if (!phaseId || !name) {
    showToast('Phase and name required', 'error');
    return;
  }

  // ... rest of save logic
}
```

### Task 3: Fix All Colors to Match Style Guide

Replace all hardcoded colors with CSS variables:

```css
/* REMOVE - Light theme colors */
/* These should NOT exist anymore */
background: #ffffff;  /* BAD */
background: #f6f7fb;  /* BAD */
color: #323338;       /* BAD */

/* REPLACE WITH - Dark theme variables */
background: var(--card);
background: var(--background);
color: var(--foreground);
```

Update all buttons to use standard classes:

```html
<!-- BEFORE -->
<button style="background: #0073ea; color: white;">Save</button>

<!-- AFTER -->
<button class="btn btn-primary">Save</button>
```

### Task 4: Fix Form Styling

Replace all form inputs:

```html
<!-- BEFORE -->
<input type="text" class="form-control">

<!-- AFTER -->
<input type="text" class="form-input">
```

Update form labels:

```html
<!-- BEFORE -->
<label for="name">Name *</label>

<!-- AFTER -->
<label class="form-label">Name <span class="required">*</span></label>
```

### Task 5: Fix Modal Structure

Ensure all modals follow standard structure:

```html
<div class="modal-backdrop" id="phaseModalBackdrop" style="display: none;">
  <div class="modal modal-md">
    <div class="modal-header">
      <h2 class="modal-title">Add Phase</h2>
      <button class="modal-close" onclick="closePhaseModal()">&times;</button>
    </div>
    <div class="modal-body">
      <!-- Form content -->
    </div>
    <div class="modal-footer">
      <button class="btn btn-secondary" onclick="closePhaseModal()">Cancel</button>
      <button class="btn btn-primary" onclick="savePhase()">Save Phase</button>
    </div>
  </div>
</div>
```

Update modal show/hide:

```javascript
function openPhaseModal() {
  const backdrop = document.getElementById('phaseModalBackdrop');
  backdrop.style.display = 'flex';
  setTimeout(() => {
    backdrop.querySelector('.modal').classList.add('show');
  }, 10);
}

function closePhaseModal() {
  const backdrop = document.getElementById('phaseModalBackdrop');
  backdrop.querySelector('.modal').classList.remove('show');
  setTimeout(() => {
    backdrop.style.display = 'none';
  }, 200);
}
```

### Task 6: Fix Table Styling

Update table to use standard classes:

```css
.worksheet-table {
  width: 100%;
  border-collapse: collapse;
}

.worksheet-table th,
.worksheet-table td {
  padding: 0.75rem 1rem;
  text-align: left;
  border-bottom: 1px solid var(--border);
}

.worksheet-table th {
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-secondary);
  background: var(--card);
}

.worksheet-table td {
  font-size: 0.875rem;
  background: var(--background);
}

.worksheet-table tbody tr:hover {
  background: var(--card-elevated);
}

/* Hierarchy rows */
.phase-row {
  background: var(--card-elevated);
  font-weight: 700;
  border-top: 2px solid var(--border);
}

.group-row {
  background: var(--card);
  font-weight: 600;
}

.subgroup-row {
  background: var(--background);
  font-weight: 500;
}

.item-row {
  background: var(--background);
}

.item-row:hover {
  background: var(--card);
}
```

Commit:
```
git add public/js/estimates-budget.js public/estimates-budget.html
git commit -m "Apply style guide to estimates page

- Add SearchablePicker component for all dropdowns
- Replace hardcoded colors with CSS variables
- Fix button classes (btn-primary, btn-secondary, etc.)
- Fix form input classes (form-input, form-label)
- Fix modal structure (modal-backdrop, modal-md)
- Fix table styling to match style guide
- Remove all light theme colors
- Add searchable dropdowns for phases/groups/subgroups"
```

## Success Criteria
- [ ] SearchablePicker component works for all dropdowns
- [ ] No hardcoded hex colors (all CSS variables)
- [ ] Buttons use standard classes (btn-primary, etc.)
- [ ] Forms use standard classes (form-input, form-label)
- [ ] Modals use standard structure (modal-backdrop)
- [ ] Tables match style guide
- [ ] Page visually consistent with index.html
- [ ] Dark theme throughout (no light theme colors)

## Notes
- This is a visual/UX overhaul - no functional changes
- Focus on consistency with rest of application
- SearchablePicker is reusable for future features
