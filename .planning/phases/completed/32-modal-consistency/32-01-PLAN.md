---
phase: 32-modal-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/styles.css
autonomous: true
must_haves:
  truths:
    - "All modal footers use consistent left/right structure"
    - "All close buttons use single .close-btn class"
    - "Modal title classes are unified to .modal-title-row"
    - "Button gaps are consistent across all modal footers"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Standardized modal CSS classes"
  key_links: []
---

<objective>
Standardize modal CSS classes and fix inconsistencies.

Purpose: Create a unified CSS foundation for all modals before fixing HTML/JS.
Output: Consistent modal CSS classes with deprecated classes aliased for backward compatibility.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize modal title classes</name>
  <files>public/css/styles.css</files>
  <action>
Find all modal title class definitions and standardize on `.modal-title-row`:

1. Find `.modal-title` styles (~line 3735) - keep as alias
2. Find `.modal-title-row` styles - this is the standard
3. Find `.modal-title-group` styles - alias to modal-title-row

Add unified selector at the appropriate location:
```css
/* Modal title row - standard structure */
.modal-title-row,
.modal-title-group,
.modal-header .modal-title {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  flex: 1;
}

.modal-title-row h2,
.modal-title-group h2 {
  margin: 0;
  font-size: 1.25rem;
  font-weight: 600;
}
```

This creates backward compatibility while standardizing on `.modal-title-row`.
  </action>
  <verify>Grep for modal-title-row in styles.css - should find unified definition</verify>
  <done>Modal title classes unified with backward compatibility</done>
</task>

<task type="auto">
  <name>Task 2: Standardize modal footer structure</name>
  <files>public/css/styles.css</files>
  <action>
Ensure modal footer left/right structure is properly defined and consistent:

1. Find `.modal-footer` styles (~line 1674) - note gap value
2. Find `.modal-footer-left` and `.modal-footer-right` styles (~line 2898)
3. Standardize gap to 0.5rem across all footer elements

Update/add styles:
```css
/* Modal footer - standard structure */
.modal-footer {
  display: flex;
  justify-content: flex-end;
  align-items: center;
  gap: 0.5rem;
  padding: 1rem 1.5rem;
  border-top: 1px solid var(--border);
  background: var(--card);
}

/* Footer with left/right sections */
.modal-footer-left,
.modal-footer-right {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.modal-footer-left {
  margin-right: auto;
}
```

Ensure all modal footer definitions use consistent 0.5rem gap.
  </action>
  <verify>Grep for "modal-footer" and check gap values are 0.5rem</verify>
  <done>Modal footer structure standardized with consistent gaps</done>
</task>

<task type="auto">
  <name>Task 3: Standardize close button class</name>
  <files>public/css/styles.css</files>
  <action>
Unify close button styling to single `.close-btn` class:

1. Find `.close-btn` styles (~line 1643-1663)
2. Find `.modal-close` styles (~line 2654-2665)
3. Make `.modal-close` an alias for `.close-btn`

Update styles:
```css
/* Close button - standard for all modals */
.close-btn,
.modal-close {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  border: none;
  background: transparent;
  color: var(--muted-foreground);
  font-size: 1.5rem;
  cursor: pointer;
  border-radius: var(--radius);
  transition: background 0.15s, color 0.15s;
}

.close-btn:hover,
.modal-close:hover {
  background: var(--muted);
  color: var(--foreground);
}
```

This ensures both classes work identically.
  </action>
  <verify>Grep for close-btn and modal-close - both should share same styles</verify>
  <done>Close button classes unified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] .modal-title-row, .modal-title-group unified in CSS
- [ ] .modal-footer gap is 0.5rem everywhere
- [ ] .close-btn and .modal-close share identical styles
- [ ] No CSS syntax errors (check with browser dev tools)
</verification>

<success_criteria>
- All modal CSS classes standardized
- Backward compatibility maintained through aliases
- Consistent spacing and sizing across modal elements
</success_criteria>

<output>
After completion, create `.planning/phases/32-modal-consistency/32-01-SUMMARY.md`
</output>
