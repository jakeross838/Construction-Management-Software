---
phase: 110-ui-ux-overhaul
plan: 04
type: execute
wave: 3
depends_on: [110-02]
files_modified:
  - public/js/estimates-budget.js
  - public/css/styles.css
autonomous: true

must_haves:
  truths:
    - "Tab key moves between editable cells in table"
    - "Shift+Tab moves backward between cells"
    - "Enter saves and moves to next row"
    - "Escape cancels edit"
    - "Ctrl/Cmd+S saves current estimate"
    - "Keyboard shortcuts work consistently"
  artifacts:
    - path: "public/js/estimates-budget.js"
      provides: "Global keyboard shortcut handlers"
      contains: "addEventListener.*keydown"
    - path: "public/css/styles.css"
      provides: "Keyboard shortcut help styling"
      contains: "shortcut-hint"
  key_links:
    - from: "document keydown listener"
      to: "keyboard handler functions"
      via: "event key matching"
      pattern: "e\\.key.*===|e\\.ctrlKey|e\\.metaKey"
---

<objective>
Add keyboard shortcuts for power users to navigate and edit efficiently.

Purpose: Address requirement UX-05 "Keyboard shortcuts for power users" to enable fast data entry without mouse, similar to spreadsheet applications.

Output: Working keyboard shortcuts (Tab, Enter, Escape, Ctrl+S, etc.) and optional help indicator showing available shortcuts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/110-ui-ux-overhaul/110-RESEARCH.md
@.planning/phases/110-ui-ux-overhaul/110-02-SUMMARY.md

Reference files:
@public/js/estimates-budget.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement global keyboard shortcuts</name>
  <files>public/js/estimates-budget.js</files>
  <action>
    Add global keyboard shortcut handlers for common actions.

    ```javascript
    // ============================================================
    // KEYBOARD SHORTCUTS
    // ============================================================

    const KEYBOARD_SHORTCUTS = {
      'ctrl+s': { action: saveCurrentEstimate, description: 'Save estimate' },
      'ctrl+n': { action: openAddLineModal, description: 'Add new line item' },
      'ctrl+shift+n': { action: openCreateModal, description: 'New estimate' },
      'escape': { action: handleEscapeKey, description: 'Close modal / Cancel edit' },
      '?': { action: toggleShortcutHelp, description: 'Show keyboard shortcuts' },
      'ctrl+/': { action: toggleShortcutHelp, description: 'Show keyboard shortcuts' },
    };

    function initKeyboardShortcuts() {
      document.addEventListener('keydown', handleGlobalKeydown);
    }

    function handleGlobalKeydown(e) {
      // Don't trigger shortcuts when typing in inputs (unless it's Escape)
      const isTyping = e.target.matches('input, textarea, [contenteditable="true"]');

      if (isTyping && e.key !== 'Escape') {
        return;
      }

      // Build shortcut key string
      const key = buildShortcutKey(e);

      // Check if we have a handler for this shortcut
      const shortcut = KEYBOARD_SHORTCUTS[key];
      if (shortcut) {
        e.preventDefault();
        shortcut.action();
      }
    }

    function buildShortcutKey(e) {
      const parts = [];

      if (e.ctrlKey || e.metaKey) parts.push('ctrl');
      if (e.shiftKey) parts.push('shift');
      if (e.altKey) parts.push('alt');

      // Normalize key
      let key = e.key.toLowerCase();
      if (key === ' ') key = 'space';

      parts.push(key);

      return parts.join('+');
    }

    function saveCurrentEstimate() {
      // If in detail modal and editing, save the estimate
      if (currentEstimate) {
        // Trigger save of any pending inline edits
        const editingCell = document.querySelector('.editable-cell.editing');
        if (editingCell && editingCell._inlineEdit) {
          editingCell._inlineEdit.save();
        }
        showToast('Estimate saved', 'success');
      } else {
        // If in create modal, trigger save
        const createModal = document.getElementById('estimateModal');
        if (createModal && createModal.classList.contains('show')) {
          saveEstimate();
        }
      }
    }

    function handleEscapeKey() {
      // Priority: Close edit mode -> Close modal -> Close dropdown

      // 1. If editing a cell, cancel edit
      const editingCell = document.querySelector('.editable-cell.editing');
      if (editingCell && editingCell._inlineEdit) {
        editingCell._inlineEdit.cancel();
        return;
      }

      // 2. Close any open dropdown
      const openDropdown = document.querySelector('.dropdown.open');
      if (openDropdown) {
        openDropdown.classList.remove('open');
        return;
      }

      // 3. Close any open modal
      closeAllModals();
    }

    let shortcutHelpVisible = false;

    function toggleShortcutHelp() {
      const existingHelp = document.getElementById('shortcutHelpPanel');

      if (existingHelp) {
        existingHelp.remove();
        shortcutHelpVisible = false;
        return;
      }

      showShortcutHelp();
    }

    function showShortcutHelp() {
      // Create help panel
      const panel = document.createElement('div');
      panel.id = 'shortcutHelpPanel';
      panel.className = 'shortcut-help-panel';

      panel.innerHTML = `
        <div class="shortcut-help-header">
          <h3>Keyboard Shortcuts</h3>
          <button class="btn btn-ghost btn-sm" onclick="toggleShortcutHelp()">&times;</button>
        </div>
        <div class="shortcut-help-body">
          <div class="shortcut-section">
            <h4>Navigation</h4>
            <div class="shortcut-item">
              <span class="shortcut-keys"><kbd>Tab</kbd></span>
              <span class="shortcut-desc">Next cell</span>
            </div>
            <div class="shortcut-item">
              <span class="shortcut-keys"><kbd>Shift</kbd> + <kbd>Tab</kbd></span>
              <span class="shortcut-desc">Previous cell</span>
            </div>
            <div class="shortcut-item">
              <span class="shortcut-keys"><kbd>Enter</kbd></span>
              <span class="shortcut-desc">Save & next row</span>
            </div>
            <div class="shortcut-item">
              <span class="shortcut-keys"><kbd>Esc</kbd></span>
              <span class="shortcut-desc">Cancel edit / Close</span>
            </div>
          </div>
          <div class="shortcut-section">
            <h4>Actions</h4>
            <div class="shortcut-item">
              <span class="shortcut-keys"><kbd>Ctrl</kbd> + <kbd>S</kbd></span>
              <span class="shortcut-desc">Save estimate</span>
            </div>
            <div class="shortcut-item">
              <span class="shortcut-keys"><kbd>Ctrl</kbd> + <kbd>N</kbd></span>
              <span class="shortcut-desc">Add line item</span>
            </div>
            <div class="shortcut-item">
              <span class="shortcut-keys"><kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>N</kbd></span>
              <span class="shortcut-desc">New estimate</span>
            </div>
          </div>
          <div class="shortcut-section">
            <h4>Help</h4>
            <div class="shortcut-item">
              <span class="shortcut-keys"><kbd>?</kbd></span>
              <span class="shortcut-desc">Show this help</span>
            </div>
          </div>
        </div>
      `;

      document.body.appendChild(panel);
      shortcutHelpVisible = true;

      // Close on click outside
      setTimeout(() => {
        document.addEventListener('click', closeShortcutHelpOnClickOutside, { once: true });
      }, 100);
    }

    function closeShortcutHelpOnClickOutside(e) {
      const panel = document.getElementById('shortcutHelpPanel');
      if (panel && !panel.contains(e.target)) {
        panel.remove();
        shortcutHelpVisible = false;
      }
    }
    ```

    Add initKeyboardShortcuts() call in DOMContentLoaded:
    ```javascript
    document.addEventListener('DOMContentLoaded', async () => {
      // ... existing initialization ...

      // Initialize keyboard shortcuts
      initKeyboardShortcuts();
    });
    ```
  </action>
  <verify>
    - Ctrl+S triggers save
    - Ctrl+N opens add line item (when in detail modal)
    - Escape closes modals and cancels edits
    - ? key shows shortcut help panel
    - Shortcuts don't fire when typing in inputs
  </verify>
  <done>
    Global keyboard shortcuts implemented for save, add, escape, and help. Shortcuts respect input focus state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for shortcut help panel</name>
  <files>public/css/styles.css</files>
  <action>
    Add CSS styling for the keyboard shortcut help panel.

    ```css
    /* ============================================================
       KEYBOARD SHORTCUT HELP
       ============================================================ */

    .shortcut-help-panel {
      position: fixed;
      bottom: 24px;
      right: 24px;
      width: 320px;
      max-height: 80vh;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 12px;
      box-shadow: var(--shadow-lg);
      z-index: 9999;
      overflow: hidden;
      animation: slideUp 0.2s ease;
    }

    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(16px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .shortcut-help-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 16px 20px;
      border-bottom: 1px solid var(--border);
    }

    .shortcut-help-header h3 {
      margin: 0;
      font-size: 1rem;
      font-weight: 600;
    }

    .shortcut-help-body {
      padding: 16px 20px;
      max-height: 60vh;
      overflow-y: auto;
    }

    .shortcut-section {
      margin-bottom: 20px;
    }

    .shortcut-section:last-child {
      margin-bottom: 0;
    }

    .shortcut-section h4 {
      margin: 0 0 12px 0;
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--muted-foreground);
    }

    .shortcut-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 8px 0;
    }

    .shortcut-keys {
      display: flex;
      gap: 4px;
      align-items: center;
    }

    .shortcut-desc {
      color: var(--muted-foreground);
      font-size: 0.9rem;
    }

    kbd {
      display: inline-block;
      padding: 3px 8px;
      font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
      font-size: 0.75rem;
      font-weight: 500;
      background: var(--muted);
      border: 1px solid var(--border);
      border-radius: 4px;
      color: var(--foreground);
      box-shadow: 0 1px 2px rgba(0,0,0,0.05);
    }

    /* Shortcut hint in footer */
    .shortcut-hint {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 12px;
      background: var(--accent);
      border-radius: 6px;
      font-size: 0.8rem;
      color: var(--muted-foreground);
      cursor: pointer;
      transition: background 0.15s;
    }

    .shortcut-hint:hover {
      background: var(--muted);
    }

    /* Mobile: Hide keyboard shortcut hint */
    @media (max-width: 768px) {
      .shortcut-hint {
        display: none;
      }

      .shortcut-help-panel {
        left: 16px;
        right: 16px;
        bottom: 16px;
        width: auto;
      }
    }
    ```
  </action>
  <verify>
    - .shortcut-help-panel positioned fixed at bottom-right
    - Panel has slide-up animation
    - kbd elements styled as keyboard keys
    - Sections organized with headers
    - Mobile responsive
  </verify>
  <done>
    Shortcut help panel CSS complete with keyboard key styling and responsive behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add keyboard shortcut hint to footer</name>
  <files>public/estimates-budget.html</files>
  <action>
    Add a subtle hint in the page footer or modal footer showing "Press ? for shortcuts".

    In the estimate detail modal footer, add a shortcut hint:

    ```html
    <div class="modal-footer">
      <span class="shortcut-hint" onclick="toggleShortcutHelp()">
        <kbd>?</kbd> Keyboard shortcuts
      </span>
      <div class="footer-spacer"></div>
      <button class="btn btn-secondary" onclick="closeDetailModal()">Close</button>
      <!-- ... other buttons ... -->
    </div>
    ```

    Add CSS for footer spacer:
    ```css
    .footer-spacer {
      flex: 1;
    }
    ```

    Also add the hint to the worksheet footer in the Line Items tab:
    ```html
    <div class="worksheet-footer">
      <div class="worksheet-legend">
        <span class="legend-item">LS = Lump Sum</span>
        <span class="legend-item">EA = Each</span>
        <span class="legend-item">SF = Sq Ft</span>
        <span class="legend-item">LF = Linear Ft</span>
        <span class="shortcut-hint" onclick="toggleShortcutHelp()" style="margin-left: auto;">
          <kbd>?</kbd> Shortcuts
        </span>
      </div>
      <!-- ... totals ... -->
    </div>
    ```
  </action>
  <verify>
    - Shortcut hint visible in modal footer
    - Shortcut hint visible in worksheet legend area
    - Clicking hint opens shortcut help panel
    - Hidden on mobile (via CSS)
  </verify>
  <done>
    Shortcut hints added to modal footer and worksheet footer. Users have discoverable path to keyboard shortcuts.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Press ? key anywhere - shortcut help panel should appear
2. Press Ctrl+S when viewing estimate - should show "Estimate saved" toast
3. Press Escape when editing a cell - should cancel edit
4. Press Escape when modal open - should close modal
5. Tab/Shift+Tab should navigate between editable cells
6. Shortcut hint visible in footer areas
7. Mobile: shortcut hint hidden (touch users don't need keyboard shortcuts)
</verification>

<success_criteria>
- [ ] Global keyboard shortcuts work (Ctrl+S, Escape, ?)
- [ ] Shortcut help panel shows all available shortcuts
- [ ] Panel can be dismissed with Escape or clicking outside
- [ ] kbd elements styled as keyboard keys
- [ ] Shortcut hints visible in footer areas
- [ ] Shortcuts don't interfere with typing in inputs
</success_criteria>

<output>
After completion, create `.planning/phases/110-ui-ux-overhaul/110-04-SUMMARY.md`
</output>
