---
phase: 110-ui-ux-overhaul
plan: 05
type: execute
wave: 3
depends_on: [110-01, 110-02]
files_modified:
  - public/css/styles.css
  - public/estimates-budget.html
autonomous: false

must_haves:
  truths:
    - "Line items display as cards on mobile (< 640px)"
    - "Table headers stick on tablet horizontal scroll"
    - "Touch targets are at least 44px"
    - "Page is usable on tablet in field conditions"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Responsive CSS for mobile card view"
      contains: "@media.*max-width.*640px"
    - path: "public/estimates-budget.html"
      provides: "Dual-render structure for table and cards"
      contains: "line-item-card"
  key_links:
    - from: "CSS media queries"
      to: "HTML structure"
      via: "display: none toggles"
      pattern: "@media.*display.*none"
---

<objective>
Implement responsive design for tablet and mobile field use.

Purpose: Address requirement UX-06 "Mobile-friendly responsive design" to enable field workers to use estimates on tablets and phones at job sites.

Output: Mobile-first responsive design with card layout for line items on small screens, sticky headers on tablet, and proper touch targets.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/110-ui-ux-overhaul/110-RESEARCH.md
@.planning/phases/110-ui-ux-overhaul/110-01-SUMMARY.md
@.planning/phases/110-ui-ux-overhaul/110-02-SUMMARY.md

Reference files:
@public/css/styles.css
@public/estimates-budget.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile card view HTML structure</name>
  <files>public/estimates-budget.html</files>
  <action>
    Add a card-based alternative view for line items that displays on mobile. This sits alongside the table but is hidden on desktop.

    In the Line Items tab (tab-lines), after the worksheet-table-wrap div, add:

    ```html
    <!-- Mobile Card View (visible < 640px) -->
    <div class="line-items-cards" id="lineItemsCards">
      <!-- Populated by JS with same data as table -->
    </div>
    ```

    Update renderLineItems in JS to also populate the card view:

    ```javascript
    function renderLineItemCards(lines) {
      const container = document.getElementById('lineItemsCards');
      if (!container) return;

      if (!lines || lines.length === 0) {
        container.innerHTML = `
          <div class="empty-state-card">
            <p>No line items yet</p>
            <button class="btn btn-primary" onclick="openAddLineModal()">+ Add First Item</button>
          </div>
        `;
        return;
      }

      container.innerHTML = lines.map((line, index) => `
        <div class="line-item-card" data-id="${line.id}">
          <div class="line-card-header">
            <span class="line-card-number">#${index + 1}</span>
            <span class="line-card-code">${escapeHtml(line.cost_code?.code || '-')}</span>
            <button class="btn btn-icon btn-ghost" onclick="deleteLineItem('${line.id}')">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M5.5 5.5A.5.5 0 016 6v6a.5.5 0 01-1 0V6a.5.5 0 01.5-.5zm2.5 0a.5.5 0 01.5.5v6a.5.5 0 01-1 0V6a.5.5 0 01.5-.5zm3 .5a.5.5 0 00-1 0v6a.5.5 0 001 0V6z"/>
                <path fill-rule="evenodd" d="M14.5 3a1 1 0 01-1 1H13v9a2 2 0 01-2 2H5a2 2 0 01-2-2V4h-.5a1 1 0 01-1-1V2a1 1 0 011-1H6a1 1 0 011-1h2a1 1 0 011 1h3.5a1 1 0 011 1v1zM4.118 4L4 4.059V13a1 1 0 001 1h6a1 1 0 001-1V4.059L11.882 4H4.118zM2.5 3V2h11v1h-11z"/>
              </svg>
            </button>
          </div>
          <div class="line-card-description"
               data-editable
               data-field="description"
               data-type="text"
               data-id="${line.id}">
            ${escapeHtml(line.description || 'No description')}
          </div>
          <div class="line-card-details">
            <div class="line-card-field">
              <span class="field-label">Qty</span>
              <span class="field-value"
                    data-editable
                    data-field="quantity"
                    data-type="number"
                    data-id="${line.id}">
                ${line.quantity || 1}
              </span>
              <span class="field-unit">${escapeHtml(line.unit || 'LS')}</span>
            </div>
            <div class="line-card-field">
              <span class="field-label">Unit Cost</span>
              <span class="field-value"
                    data-editable
                    data-field="unit_cost"
                    data-type="currency"
                    data-id="${line.id}">
                ${formatCurrency(line.unit_cost || 0)}
              </span>
            </div>
            <div class="line-card-field line-card-amount">
              <span class="field-label">Amount</span>
              <span class="field-value">${formatCurrency(line.amount || 0)}</span>
            </div>
          </div>
        </div>
      `).join('');

      // Initialize inline editing on card fields
      initInlineEditing();
    }
    ```

    Update renderLineItems to call both:
    ```javascript
    function renderLineItems(lines) {
      // Render table view (existing)
      renderLineItemsTable(lines);

      // Also render card view for mobile
      renderLineItemCards(lines);
    }
    ```
  </action>
  <verify>
    - #lineItemsCards container exists in HTML
    - renderLineItemCards function creates card elements
    - Cards have data-editable attributes for inline editing
    - Both table and cards render the same data
  </verify>
  <done>
    Mobile card view HTML structure added. Line items render in both table (desktop) and card (mobile) formats.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add responsive CSS for table/card switching</name>
  <files>public/css/styles.css</files>
  <action>
    Add comprehensive responsive CSS to switch between table and card views, plus touch-friendly improvements.

    ```css
    /* ============================================================
       RESPONSIVE LINE ITEMS
       ============================================================ */

    /* Desktop: Show table, hide cards */
    @media (min-width: 1024px) {
      .worksheet-table-wrap {
        display: block;
      }

      .line-items-cards {
        display: none;
      }
    }

    /* Tablet: Table with horizontal scroll and sticky first column */
    @media (min-width: 641px) and (max-width: 1023px) {
      .worksheet-table-wrap {
        display: block;
        overflow-x: auto;
        -webkit-overflow-scrolling: touch;
      }

      .worksheet-table {
        min-width: 800px;
      }

      /* Sticky description column */
      .worksheet-table th:nth-child(5),
      .worksheet-table td:nth-child(5) {
        position: sticky;
        left: 0;
        background: var(--card);
        z-index: 5;
        box-shadow: 2px 0 4px rgba(0,0,0,0.05);
      }

      .worksheet-table tbody tr:hover td:nth-child(5) {
        background: var(--accent);
      }

      .line-items-cards {
        display: none;
      }
    }

    /* Mobile: Hide table, show cards */
    @media (max-width: 640px) {
      .worksheet-table-wrap {
        display: none;
      }

      .line-items-cards {
        display: flex;
        flex-direction: column;
        gap: 12px;
        padding: 12px;
      }
    }

    /* Mobile Card Styling */
    .line-item-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 16px;
      transition: box-shadow 0.15s;
    }

    .line-item-card:active {
      box-shadow: 0 0 0 2px var(--primary);
    }

    .line-card-header {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 12px;
    }

    .line-card-number {
      font-size: 0.8rem;
      font-weight: 600;
      color: var(--muted-foreground);
    }

    .line-card-code {
      padding: 4px 10px;
      background: var(--accent);
      border-radius: 6px;
      font-family: 'SF Mono', monospace;
      font-size: 0.8rem;
    }

    .line-card-header .btn-icon {
      margin-left: auto;
    }

    .line-card-description {
      font-weight: 500;
      margin-bottom: 16px;
      padding: 8px;
      border-radius: 6px;
      min-height: 44px;
      display: flex;
      align-items: center;
    }

    .line-card-description[data-editable]:hover {
      background: var(--accent);
    }

    .line-card-details {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
    }

    .line-card-field {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .line-card-field .field-label {
      font-size: 0.75rem;
      color: var(--muted-foreground);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .line-card-field .field-value {
      font-weight: 500;
      padding: 8px;
      border-radius: 6px;
      min-height: 44px;
      display: flex;
      align-items: center;
    }

    .line-card-field .field-value[data-editable]:hover {
      background: var(--accent);
    }

    .line-card-field .field-unit {
      font-size: 0.85rem;
      color: var(--muted-foreground);
      margin-left: 4px;
    }

    .line-card-amount {
      grid-column: 1 / -1;
      background: var(--accent);
      padding: 12px;
      border-radius: 8px;
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }

    .line-card-amount .field-value {
      font-size: 1.1rem;
      font-weight: 600;
      font-family: 'SF Mono', monospace;
      padding: 0;
      min-height: auto;
    }

    /* Empty state for cards */
    .empty-state-card {
      text-align: center;
      padding: 48px 24px;
      background: var(--card);
      border: 2px dashed var(--border);
      border-radius: 12px;
    }

    .empty-state-card p {
      margin-bottom: 16px;
      color: var(--muted-foreground);
    }

    /* Touch-friendly improvements */
    @media (max-width: 768px) {
      /* Larger touch targets */
      .btn {
        min-height: 44px;
        min-width: 44px;
      }

      .btn-sm {
        min-height: 40px;
        padding: 8px 16px;
      }

      /* Larger form controls */
      .form-control {
        min-height: 44px;
        font-size: 16px; /* Prevents iOS zoom */
      }

      select.form-control {
        padding: 10px 12px;
      }

      /* Larger checkboxes */
      input[type="checkbox"] {
        width: 24px;
        height: 24px;
      }

      /* More spacing */
      .modal-body {
        padding: 16px;
      }

      .modal-header {
        padding: 16px;
      }

      .modal-footer {
        padding: 16px;
        flex-wrap: wrap;
        gap: 8px;
      }

      /* Stack footer buttons on mobile */
      .modal-footer .btn {
        flex: 1 1 auto;
      }

      /* Adjust dropdown position for mobile */
      .dropdown-menu {
        position: fixed;
        left: 16px !important;
        right: 16px !important;
        bottom: 16px;
        top: auto !important;
        width: auto;
        max-height: 50vh;
        overflow-y: auto;
        border-radius: 12px;
      }

      /* Add bottom sheet animation */
      .dropdown.open .dropdown-menu {
        transform: translateY(0);
        animation: slideUpSheet 0.2s ease;
      }

      @keyframes slideUpSheet {
        from {
          transform: translateY(100%);
        }
        to {
          transform: translateY(0);
        }
      }
    }

    /* Tablet-specific adjustments */
    @media (min-width: 641px) and (max-width: 1023px) {
      /* Modal sizing */
      .modal-content {
        max-width: 90vw;
      }

      .modal-fullscreen-dark .modal-content {
        width: 95vw;
        max-width: none;
      }

      /* Worksheet footer */
      .worksheet-footer {
        flex-direction: column;
        align-items: stretch;
      }

      .worksheet-legend {
        flex-wrap: wrap;
        margin-bottom: 12px;
      }

      .worksheet-totals {
        align-self: flex-end;
      }
    }
    ```
  </action>
  <verify>
    - Table hidden on mobile (< 640px)
    - Cards shown on mobile
    - Table scrolls horizontally on tablet with sticky column
    - Touch targets are minimum 44px
    - Form inputs are 16px font (prevents iOS zoom)
  </verify>
  <done>
    Responsive CSS complete with mobile card view, tablet horizontal scroll, and touch-friendly sizing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Mobile-responsive estimates page with:
    - Card layout for line items on mobile screens (< 640px)
    - Horizontal scrolling table with sticky column on tablet (641-1023px)
    - Full table on desktop (1024px+)
    - Touch-friendly 44px minimum tap targets
    - Inline editing works on mobile cards
  </what-built>
  <how-to-verify>
    1. Start the server: `npm start`
    2. Open http://localhost:3001/estimates-budget.html
    3. Create or open an estimate with line items

    Desktop verification:
    - Should see full table with all columns
    - Inline editing should work

    Tablet verification (resize browser to ~800px wide):
    - Table should scroll horizontally
    - Description column should stay sticky (visible while scrolling)

    Mobile verification (resize browser to ~400px wide OR use Chrome DevTools device mode):
    - Table should be hidden
    - Line items should display as stacked cards
    - Each card shows: number, cost code, description, qty/unit, unit cost, amount
    - Tapping on description/qty/unit cost should allow inline editing
    - Buttons should be easy to tap (44px targets)
    - "Add Item" button should be large and accessible

    Also test:
    - Open on actual phone/tablet if available
    - Rotate device between portrait and landscape
  </how-to-verify>
  <resume-signal>Type "approved" if responsive design works correctly, or describe any issues with the mobile/tablet experience</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Desktop (1024px+): Full table visible, cards hidden
2. Tablet (641-1023px): Table with horizontal scroll, sticky description column
3. Mobile (< 640px): Table hidden, cards visible
4. Inline editing works in both table and card views
5. Touch targets are comfortable size
6. Forms don't trigger iOS zoom
</verification>

<success_criteria>
- [ ] Table/card view switches based on viewport width
- [ ] Mobile cards show all essential line item data
- [ ] Inline editing works on mobile card fields
- [ ] Tablet horizontal scroll works smoothly
- [ ] Touch targets are at least 44px
- [ ] Visual verification on mobile device or emulator
</success_criteria>

<output>
After completion, create `.planning/phases/110-ui-ux-overhaul/110-05-SUMMARY.md`
</output>
