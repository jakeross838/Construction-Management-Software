---
phase: 35-navigation-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/styles.css
autonomous: true
must_haves:
  truths:
    - "Sidebar active states have consistent styling"
    - "Navigation active states use unified pattern"
    - "Legacy navigation classes removed or aliased"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Unified sidebar and navigation active state CSS"
  key_links: []
---

<objective>
Standardize sidebar styling and active state patterns.

Purpose: Create consistent active state styling across sidebar and navigation components.
Output: Unified CSS for sidebar active states and navigation patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize sidebar active states</name>
  <files>public/css/styles.css</files>
  <action>
Unify sidebar active state styling:

1. Find existing `.job-item.active` and `.nav-link.active` definitions
2. Create unified active state pattern:

```css
/* Sidebar active states - unified pattern */
.job-item.active {
  background: var(--primary);
  color: var(--primary-foreground);
}

.job-item.active .job-item-icon,
.job-item.active .job-item-name,
.job-item.active .job-item-count {
  color: var(--primary-foreground);
}

.job-item.active .job-item-status {
  background: rgba(255, 255, 255, 0.2);
}

/* Hover state for non-active items */
.job-item:not(.active):hover {
  background: var(--muted);
}
```

3. Ensure active state works in both expanded and collapsed sidebar modes
  </action>
  <verify>Grep for "job-item.active" - should have unified styling with var(--primary)</verify>
  <done>Sidebar active states unified</done>
</task>

<task type="auto">
  <name>Task 2: Standardize navigation active states</name>
  <files>public/css/styles.css</files>
  <action>
Unify main navigation and sub-navigation active states:

1. Find existing `.main-nav-link.active` and `.sub-nav-link.active` definitions
2. Ensure consistent active state pattern:

```css
/* Main navigation active state */
.main-nav-link.active {
  color: var(--primary);
  background: rgba(59, 111, 212, 0.1);
  border-bottom: 2px solid var(--primary);
}

/* Sub navigation active state */
.sub-nav-link.active {
  color: var(--primary);
  background: var(--card);
  font-weight: 500;
}

/* Consistent hover for non-active nav items */
.main-nav-link:not(.active):hover,
.sub-nav-link:not(.active):hover {
  color: var(--foreground);
  background: var(--muted);
}
```

3. Remove any duplicate or conflicting active state definitions
  </action>
  <verify>Grep for "main-nav-link.active" - should have consistent styling</verify>
  <done>Navigation active states unified</done>
</task>

<task type="auto">
  <name>Task 3: Clean up legacy navigation classes</name>
  <files>public/css/styles.css</files>
  <action>
Address legacy navigation classes:

1. Find legacy classes: `.header-left`, `.header-nav`, old `.nav-link`
2. Check if they're still used in HTML files (grep public/*.html for these classes)
3. If NOT used: Remove the CSS definitions entirely
4. If used: Create aliases to the new system:

```css
/* Legacy nav classes - alias to new system */
.nav-link {
  /* Alias to main-nav-link styling */
}

.header-left {
  /* Alias to header-brand styling */
}
```

5. Add comment: "/* Legacy support - scheduled for removal */"
  </action>
  <verify>Grep public HTML files for "header-left" and "nav-link" - document if still used</verify>
  <done>Legacy navigation classes addressed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] .job-item.active has consistent primary color styling
- [ ] .main-nav-link.active has border-bottom indicator
- [ ] .sub-nav-link.active has card background
- [ ] Hover states work for non-active items
- [ ] Legacy classes removed or aliased
- [ ] No CSS syntax errors
</verification>

<success_criteria>
- Active states consistent across all navigation components
- Sidebar selection visually clear
- Navigation selection visually clear
- Legacy code cleaned up
</success_criteria>

<output>
After completion, create `.planning/phases/35-navigation-layout/35-01-SUMMARY.md`
</output>
