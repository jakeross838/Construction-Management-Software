---
phase: 68-catalog-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/catalog.html
  - public/js/catalog.js
autonomous: true

must_haves:
  truths:
    - "Staff can open Add Product modal from catalog page"
    - "Staff can fill product details and save to database"
    - "Staff can open Edit Product modal from product detail view"
    - "Staff can update existing product details"
  artifacts:
    - path: "public/catalog.html"
      provides: "Add/Edit Product modal HTML"
    - path: "public/js/catalog.js"
      provides: "Modal open/save/edit functions"
      exports: ["openAddProductModal", "openEditProductModal", "saveProduct"]
  key_links:
    - from: "public/js/catalog.js"
      to: "/api/selections/catalog"
      via: "fetch POST/PATCH"
      pattern: "fetch.*selections/catalog"
---

<objective>
Implement Add Product and Edit Product modals for catalog management.

Purpose: Allow staff to create new catalog products and edit existing ones.
Output: Working modals with form fields for all product attributes, connected to existing API endpoints.
</objective>

<context>
@public/catalog.html
@public/js/catalog.js
@server/routes/selections.js

Key existing code:
- API endpoints already exist: POST /api/selections/catalog (create), PATCH /api/selections/catalog/:id (update)
- Event listeners already set up in catalog.js for btnAddProduct and btnEditProduct
- Modal pattern uses .show class for visibility (see CLAUDE.md)
- Categories and vendors already loaded in state (allCategories, allVendors)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Product Modal HTML</name>
  <files>public/catalog.html</files>
  <action>
Add the Add/Edit Product modal HTML to catalog.html before the closing </body> tag.

The modal should include:
- Modal with id="productFormModal" class="modal"
- Header with title (changes between "Add Product" and "Edit Product")
- Form fields:
  - name (text, required)
  - category_id (select from categories)
  - vendor_id (select from vendors)
  - description (textarea)
  - model_number (text)
  - sku (text)
  - unit_price (number)
  - unit (select: each, sqft, lnft, set)
  - room (select: Kitchen, Bathroom, Living Room, Bedroom, Exterior, Other)
  - tags (text, comma-separated)
- Hidden field for id (used when editing)
- Image upload section (placeholder - will be implemented in 68-03)
- Save and Cancel buttons

Example structure:
```html
<!-- Add/Edit Product Modal -->
<div id="productFormModal" class="modal">
  <div class="modal-content" style="max-width: 600px;">
    <div class="modal-header">
      <h3 id="productFormTitle">Add Product</h3>
      <button class="btn-close" onclick="closeProductFormModal()">&times;</button>
    </div>
    <div class="modal-body">
      <form id="productForm">
        <input type="hidden" id="productFormId">

        <div class="form-row">
          <div class="form-group">
            <label for="productFormName">Product Name *</label>
            <input type="text" id="productFormName" required>
          </div>
        </div>

        <div class="form-row two-col">
          <div class="form-group">
            <label for="productFormCategory">Category *</label>
            <select id="productFormCategory" required>
              <option value="">Select category</option>
            </select>
          </div>
          <div class="form-group">
            <label for="productFormVendor">Vendor</label>
            <select id="productFormVendor">
              <option value="">Select vendor</option>
            </select>
          </div>
        </div>

        <div class="form-group">
          <label for="productFormDescription">Description</label>
          <textarea id="productFormDescription" rows="3"></textarea>
        </div>

        <div class="form-row two-col">
          <div class="form-group">
            <label for="productFormModel">Model Number</label>
            <input type="text" id="productFormModel">
          </div>
          <div class="form-group">
            <label for="productFormSku">SKU</label>
            <input type="text" id="productFormSku">
          </div>
        </div>

        <div class="form-row three-col">
          <div class="form-group">
            <label for="productFormPrice">Unit Price</label>
            <input type="number" id="productFormPrice" step="0.01" min="0">
          </div>
          <div class="form-group">
            <label for="productFormUnit">Unit</label>
            <select id="productFormUnit">
              <option value="each">Each</option>
              <option value="sqft">Sq Ft</option>
              <option value="lnft">Ln Ft</option>
              <option value="set">Set</option>
            </select>
          </div>
          <div class="form-group">
            <label for="productFormRoom">Room</label>
            <select id="productFormRoom">
              <option value="">Any</option>
              <option value="Kitchen">Kitchen</option>
              <option value="Bathroom">Bathroom</option>
              <option value="Living Room">Living Room</option>
              <option value="Bedroom">Bedroom</option>
              <option value="Exterior">Exterior</option>
              <option value="Other">Other</option>
            </select>
          </div>
        </div>

        <div class="form-group">
          <label for="productFormTags">Tags (comma-separated)</label>
          <input type="text" id="productFormTags" placeholder="modern, eco-friendly, premium">
        </div>

        <div id="productImageUpload" class="form-group">
          <label>Product Images</label>
          <div class="image-upload-area" id="imageUploadArea">
            <p>Image upload will be available after saving</p>
          </div>
        </div>
      </form>
    </div>
    <div class="modal-footer">
      <button type="button" class="btn btn-secondary" onclick="closeProductFormModal()">Cancel</button>
      <button type="button" class="btn btn-primary" onclick="saveProduct()">Save Product</button>
    </div>
  </div>
</div>
```

Add CSS for form layout in the existing style section or link to catalog.css:
```css
.form-row { margin-bottom: 1rem; }
.form-row.two-col { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
.form-row.three-col { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1rem; }
.form-group label { display: block; margin-bottom: 0.25rem; font-size: 0.875rem; color: var(--text-secondary); }
.form-group input, .form-group select, .form-group textarea {
  width: 100%;
  padding: 0.5rem;
  border: 1px solid var(--border);
  border-radius: 4px;
  background: var(--bg-primary);
  color: var(--text-primary);
}
.image-upload-area {
  border: 2px dashed var(--border);
  border-radius: 8px;
  padding: 2rem;
  text-align: center;
  color: var(--text-secondary);
}
```
  </action>
  <verify>
Open http://localhost:3001/catalog.html in browser and verify modal HTML exists (inspect element).
  </verify>
  <done>Add/Edit Product modal HTML added to catalog.html</done>
</task>

<task type="auto">
  <name>Task 2: Implement openAddProductModal and openEditProductModal</name>
  <files>public/js/catalog.js</files>
  <action>
Implement the modal open functions in catalog.js.

Add these functions:

```javascript
// ============================================================
// PRODUCT FORM MODAL
// ============================================================

function openAddProductModal() {
  // Reset form
  document.getElementById('productForm').reset();
  document.getElementById('productFormId').value = '';
  document.getElementById('productFormTitle').textContent = 'Add Product';

  // Populate category dropdown
  const categorySelect = document.getElementById('productFormCategory');
  categorySelect.innerHTML = '<option value="">Select category</option>';
  allCategories.forEach(cat => {
    categorySelect.innerHTML += `<option value="${cat.id}">${escapeHtml(cat.name)}</option>`;
    if (cat.children) {
      cat.children.forEach(child => {
        categorySelect.innerHTML += `<option value="${child.id}">&nbsp;&nbsp;${escapeHtml(child.name)}</option>`;
      });
    }
  });

  // Populate vendor dropdown
  const vendorSelect = document.getElementById('productFormVendor');
  vendorSelect.innerHTML = '<option value="">Select vendor</option>';
  allVendors.forEach(v => {
    vendorSelect.innerHTML += `<option value="${v.id}">${escapeHtml(v.name)}</option>`;
  });

  // Show modal
  const modal = document.getElementById('productFormModal');
  modal.style.display = 'flex';
  modal.classList.add('show');

  // Focus first field
  document.getElementById('productFormName').focus();
}

function openEditProductModal() {
  if (!currentProduct) {
    showToast('No product selected', 'error');
    return;
  }

  // First open modal to set up dropdowns
  openAddProductModal();

  // Change title
  document.getElementById('productFormTitle').textContent = 'Edit Product';

  // Fill in current product data
  document.getElementById('productFormId').value = currentProduct.id;
  document.getElementById('productFormName').value = currentProduct.name || '';
  document.getElementById('productFormCategory').value = currentProduct.category_id || '';
  document.getElementById('productFormVendor').value = currentProduct.vendor_id || '';
  document.getElementById('productFormDescription').value = currentProduct.description || '';
  document.getElementById('productFormModel').value = currentProduct.model_number || '';
  document.getElementById('productFormSku').value = currentProduct.sku || '';
  document.getElementById('productFormPrice').value = currentProduct.unit_price || '';
  document.getElementById('productFormUnit').value = currentProduct.unit || 'each';
  document.getElementById('productFormRoom').value = currentProduct.room || '';
  document.getElementById('productFormTags').value = (currentProduct.tags || []).join(', ');
}

function closeProductFormModal() {
  const modal = document.getElementById('productFormModal');
  modal.classList.remove('show');
  modal.style.display = 'none';
}
```
  </action>
  <verify>
1. Open http://localhost:3001/catalog.html
2. Click "Add Product" button - modal should open with empty form
3. Click a product card to open detail, then click "Edit" - modal should open with product data pre-filled
  </verify>
  <done>openAddProductModal and openEditProductModal implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement saveProduct function</name>
  <files>public/js/catalog.js</files>
  <action>
Implement the saveProduct function that handles both create and update.

```javascript
async function saveProduct() {
  const id = document.getElementById('productFormId').value;
  const isEdit = !!id;

  // Gather form data
  const name = document.getElementById('productFormName').value.trim();
  const category_id = document.getElementById('productFormCategory').value;
  const vendor_id = document.getElementById('productFormVendor').value || null;
  const description = document.getElementById('productFormDescription').value.trim();
  const model_number = document.getElementById('productFormModel').value.trim();
  const sku = document.getElementById('productFormSku').value.trim();
  const unit_price = parseFloat(document.getElementById('productFormPrice').value) || 0;
  const unit = document.getElementById('productFormUnit').value;
  const room = document.getElementById('productFormRoom').value || null;
  const tagsInput = document.getElementById('productFormTags').value.trim();
  const tags = tagsInput ? tagsInput.split(',').map(t => t.trim()).filter(t => t) : [];

  // Validation
  if (!name) {
    showToast('Product name is required', 'error');
    document.getElementById('productFormName').focus();
    return;
  }
  if (!category_id) {
    showToast('Category is required', 'error');
    document.getElementById('productFormCategory').focus();
    return;
  }

  const productData = {
    name,
    category_id,
    vendor_id,
    description,
    model_number,
    sku,
    unit_price,
    unit,
    room,
    tags
  };

  try {
    const url = isEdit ? `/api/selections/catalog/${id}` : '/api/selections/catalog';
    const method = isEdit ? 'PATCH' : 'POST';

    const res = await fetch(url, {
      method,
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(productData)
    });

    if (!res.ok) {
      const err = await res.json();
      throw new Error(err.error || 'Failed to save product');
    }

    const savedProduct = await res.json();

    showToast(isEdit ? 'Product updated' : 'Product created', 'success');
    closeProductFormModal();

    // Refresh products list
    await loadProducts();

    // If we were editing from detail view, close that too and reopen with updated data
    if (isEdit && currentProduct) {
      closeProductModal();
      await openProductDetail(savedProduct.id);
    }
  } catch (err) {
    console.error('Failed to save product:', err);
    showToast(err.message, 'error');
  }
}
```

Also ensure the toolbar has Add Product button. Check if it exists in catalog.html, if not add it:
```html
<button id="btnAddProduct" class="btn btn-primary">
  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
    <line x1="12" y1="5" x2="12" y2="19"></line>
    <line x1="5" y1="12" x2="19" y2="12"></line>
  </svg>
  Add Product
</button>
```
  </action>
  <verify>
1. Open catalog page, click Add Product
2. Fill in name "Test Product", select a category, set price to 100
3. Click Save - should see success toast and product appears in grid
4. Click the new product, click Edit
5. Change the name to "Test Product Updated"
6. Click Save - should see success toast and name updates
  </verify>
  <done>saveProduct function implemented for create and update</done>
</task>

</tasks>

<verification>
1. Navigate to http://localhost:3001/catalog.html
2. Click "Add Product" button - modal opens
3. Fill required fields (name, category) and save - product created
4. Click product card to open detail - detail modal opens
5. Click "Edit" button in detail modal - edit modal opens with data pre-filled
6. Change a field and save - product updates
7. Verify product appears in grid after both create and edit
</verification>

<success_criteria>
- Add Product modal opens and displays form
- Edit Product modal opens with current product data pre-filled
- Save creates new product via POST /api/selections/catalog
- Save updates existing product via PATCH /api/selections/catalog/:id
- Products list refreshes after save
- Form validation prevents empty name/category
</success_criteria>

<output>
After completion, create `.planning/phases/68-catalog-management/68-01-SUMMARY.md`
</output>
