---
phase: 105-estimates-budget-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - public/js/nav-sidebar.js
  - public/estimates.html
  - public/budget-builder.html
autonomous: true

must_haves:
  truths:
    - "Navigation shows 'Estimates & Budgets' link instead of separate Estimates and Budget Builder links"
    - "Clicking Estimates & Budgets link opens unified page"
    - "Old estimates.html redirects to estimates-budget.html"
    - "Old budget-builder.html redirects to estimates-budget.html?mode=budget"
  artifacts:
    - path: "public/js/nav-sidebar.js"
      provides: "Updated navigation with consolidated link"
      contains: "estimates-budget"
    - path: "public/estimates.html"
      provides: "Redirect stub to unified page"
      max_lines: 30
    - path: "public/budget-builder.html"
      provides: "Redirect stub to unified page with budget mode"
      max_lines: 30
  key_links:
    - from: "nav-sidebar.js"
      to: "estimates-budget.html"
      via: "href property"
      pattern: "estimates-budget\\.html"
---

<objective>
Update navigation and create redirects from old pages to the unified Estimates & Budgets page.

Purpose: Complete the consolidation by updating site navigation and ensuring bookmarks/links to old pages still work via redirects.

Output:
- Updated navigation menu with single "Estimates & Budgets" link
- Redirect stubs for estimates.html and budget-builder.html
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Jake/Construction-Management-Software/CLAUDE.md
@C:/Users/Jake/Construction-Management-Software/public/js/nav-sidebar.js
@C:/Users/Jake/Construction-Management-Software/.planning/phases/105-estimates-budget-consolidation/105-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update navigation menu</name>
  <files>public/js/nav-sidebar.js</files>
  <action>
Update the Pre-Construction navigation group to consolidate Estimates and Budget Builder:

1. Find the `precon` group in the navConfig (around line 39-49)

2. Replace these two items:
   ```javascript
   { id: 'estimates', label: 'Estimates', href: 'estimates.html' },
   { id: 'budget-builder', label: 'Budget Builder', href: 'budget-builder.html' },
   ```

   With this single item:
   ```javascript
   { id: 'estimates-budget', label: 'Estimates & Budgets', href: 'estimates-budget.html' },
   ```

3. The resulting precon items array should be:
   ```javascript
   items: [
     { id: 'leads', label: 'Leads', href: 'leads.html' },
     { id: 'job-profile', label: 'Job Profile', href: 'job-profile.html' },
     { id: 'bids', label: 'Bids', href: 'bids.html' },
     { id: 'estimates-budget', label: 'Estimates & Budgets', href: 'estimates-budget.html' },
     { id: 'contracts', label: 'Contracts', href: 'contracts.html' },
     { id: 'selections', label: 'Selections', href: 'selections.html' }
   ]
   ```

4. Update cache buster version on nav-sidebar.js references if present.
  </action>
  <verify>`grep "estimates-budget" public/js/nav-sidebar.js` shows the new navigation entry.</verify>
  <done>Navigation shows single "Estimates & Budgets" link in Pre-Construction group, Budget Builder link removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create redirect stub for estimates.html</name>
  <files>public/estimates.html</files>
  <action>
Replace the entire estimates.html with a minimal redirect page:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="refresh" content="0; url=estimates-budget.html">
  <title>Redirecting...</title>
  <script>
    // Preserve any query params
    const params = window.location.search;
    window.location.replace('estimates-budget.html' + params);
  </script>
</head>
<body>
  <p>Redirecting to <a href="estimates-budget.html">Estimates & Budgets</a>...</p>
</body>
</html>
```

This ensures:
- Immediate redirect via meta refresh
- Query parameters preserved (e.g., ?estimate_id=xxx)
- Fallback link if JS disabled
  </action>
  <verify>Open http://localhost:3001/estimates.html - should redirect to estimates-budget.html</verify>
  <done>estimates.html redirects to estimates-budget.html preserving query parameters.</done>
</task>

<task type="auto">
  <name>Task 3: Create redirect stub for budget-builder.html</name>
  <files>public/budget-builder.html</files>
  <action>
Replace the entire budget-builder.html with a minimal redirect page that opens Budget mode:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="refresh" content="0; url=estimates-budget.html?mode=budget">
  <title>Redirecting...</title>
  <script>
    // Set budget mode in localStorage and redirect
    localStorage.setItem('estimatesBudgetMode', 'budget');
    // Preserve any query params
    const params = new URLSearchParams(window.location.search);
    params.set('mode', 'budget');
    window.location.replace('estimates-budget.html?' + params.toString());
  </script>
</head>
<body>
  <p>Redirecting to <a href="estimates-budget.html?mode=budget">Estimates & Budgets (Budget Mode)</a>...</p>
</body>
</html>
```

This ensures:
- Sets localStorage mode to 'budget' before redirect
- Adds mode=budget query param
- Preserves any existing query params (e.g., ?job=xxx)
  </action>
  <verify>Open http://localhost:3001/budget-builder.html - should redirect to estimates-budget.html in Budget mode</verify>
  <done>budget-builder.html redirects to estimates-budget.html?mode=budget and sets Budget mode in localStorage.</done>
</task>

</tasks>

<verification>
1. Refresh any page - navigation should show "Estimates & Budgets" in Pre-Construction
2. Click "Estimates & Budgets" - should open unified page in Estimates mode
3. Navigate to /estimates.html directly - should redirect to /estimates-budget.html
4. Navigate to /budget-builder.html directly - should redirect to /estimates-budget.html in Budget mode
5. Test with query params: /estimates.html?job_id=xxx - should preserve params
</verification>

<success_criteria>
- [ ] Navigation shows single "Estimates & Budgets" link (not separate Estimates and Budget Builder)
- [ ] estimates.html redirects to estimates-budget.html
- [ ] budget-builder.html redirects to estimates-budget.html?mode=budget
- [ ] Query parameters preserved during redirects
- [ ] No broken links in application
</success_criteria>

<output>
After completion, create `.planning/phases/105-estimates-budget-consolidation/105-02-SUMMARY.md`
</output>
