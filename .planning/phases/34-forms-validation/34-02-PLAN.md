---
phase: 34-forms-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/styles.css
autonomous: true
must_haves:
  truths:
    - "Error messages have consistent red styling"
    - "Error display uses standard .field-error pattern"
    - "Success feedback uses toast system"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Unified error display CSS"
  key_links: []
---

<objective>
Standardize error message and validation feedback CSS.

Purpose: Create consistent error display patterns across all forms.
Output: Unified CSS for error messages and validation states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify field error styling</name>
  <files>public/css/styles.css</files>
  <action>
Consolidate all error-related CSS:

1. Find existing .field-error, .error-text, .error-state definitions
2. Create unified error styling:

```css
/* Field-level error message - standard pattern */
.field-error,
.error-text {
  font-size: 0.8rem;
  color: var(--destructive);
  margin-top: 0.25rem;
  display: none;
}

.field-error.visible,
.error-text.visible {
  display: block;
}

/* Input with error state */
.form-control.error,
.form-control.has-error,
.form-group.has-error .form-control {
  border-color: var(--destructive);
}

.form-control.error:focus,
.form-control.has-error:focus {
  border-color: var(--destructive);
  box-shadow: 0 0 0 2px rgba(220, 38, 38, 0.1);
}
```
  </action>
  <verify>Grep for "field-error" - should have unified styling</verify>
  <done>Field error styling unified</done>
</task>

<task type="auto">
  <name>Task 2: Standardize form control states</name>
  <files>public/css/styles.css</files>
  <action>
Create consistent form control states:

1. Find existing .form-control definitions
2. Ensure unified states:

```css
/* Form control - base state */
.form-control {
  width: 100%;
  padding: 0.5rem 0.75rem;
  font-size: 0.875rem;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--card);
  color: var(--foreground);
  transition: border-color 0.15s, box-shadow 0.15s;
}

/* Focus state */
.form-control:focus {
  outline: none;
  border-color: var(--primary);
  box-shadow: 0 0 0 2px rgba(59, 111, 212, 0.1);
}

/* Disabled state */
.form-control:disabled,
.form-control[readonly] {
  background: var(--muted);
  cursor: not-allowed;
  opacity: 0.7;
}

/* Valid state (optional - for explicit validation feedback) */
.form-control.valid,
.form-control.success {
  border-color: var(--success);
}
```
  </action>
  <verify>Grep for "form-control:focus" - should have unified styling</verify>
  <done>Form control states standardized</done>
</task>

<task type="auto">
  <name>Task 3: Add form helper text styling</name>
  <files>public/css/styles.css</files>
  <action>
Standardize helper/hint text below fields:

1. Find existing .form-helper, .form-hint definitions
2. Create unified helper styling:

```css
/* Form helper text - below field hints */
.form-helper,
.form-hint,
.field-hint {
  font-size: 0.75rem;
  color: var(--muted-foreground);
  margin-top: 0.25rem;
}

/* Helper with icon */
.form-helper-icon {
  display: flex;
  align-items: center;
  gap: 0.25rem;
  font-size: 0.75rem;
  color: var(--muted-foreground);
  margin-top: 0.25rem;
}
```
  </action>
  <verify>Grep for "form-helper" - should have unified styling</verify>
  <done>Form helper text standardized</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] .field-error has consistent color (var(--destructive))
- [ ] .field-error hidden by default, shown with .visible
- [ ] .form-control has focus, disabled, error states
- [ ] .form-helper has consistent styling
- [ ] No CSS syntax errors
</verification>

<success_criteria>
- Error messages display consistently
- Form controls have all necessary states
- Helper text has uniform appearance
- Validation feedback is clear
</success_criteria>

<output>
After completion, create `.planning/phases/34-forms-validation/34-02-SUMMARY.md`
</output>
