---
phase: 34-forms-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/styles.css
autonomous: true
must_haves:
  truths:
    - "Labels have consistent styling across all forms"
    - "Required indicators use standard .required class with styling"
    - "Form groups have unified structure"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Unified form label and structure CSS"
  key_links: []
---

<objective>
Standardize form label and structure CSS.

Purpose: Create consistent form foundation with unified labels and required indicators.
Output: Unified CSS for form labels, required indicators, and form group structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify label styling</name>
  <files>public/css/styles.css</files>
  <action>
Find all label-related CSS and create unified styling:

1. Search for existing label definitions (.form-group label, .form-label, .form-panel label)
2. Create unified label styling:

```css
/* Form labels - unified styling */
.form-group label,
.form-label,
.form-panel .form-group label {
  display: block;
  margin-bottom: 0.375rem;
  font-size: 0.8125rem;
  font-weight: 500;
  color: var(--foreground);
}
```

3. Ensure dark panel labels have appropriate contrast:

```css
/* Form labels in dark contexts */
.modal-fullscreen-dark .form-group label,
.form-panel .form-group label {
  color: var(--muted-foreground);
}
```
  </action>
  <verify>Grep for "form-group label" - should find unified selector</verify>
  <done>Label styling unified across all form contexts</done>
</task>

<task type="auto">
  <name>Task 2: Standardize required field indicators</name>
  <files>public/css/styles.css</files>
  <action>
Create consistent required field indicator styling:

1. Find any existing .required styling
2. Add standardized required indicator CSS:

```css
/* Required field indicator */
.required {
  color: var(--destructive);
  font-weight: 500;
  margin-left: 0.125rem;
}

/* Alternative: asterisk after label text */
label.required::after,
.form-group.required label::after {
  content: " *";
  color: var(--destructive);
  font-weight: 500;
}
```

3. Ensure required indicators visible in both light and dark contexts
  </action>
  <verify>Grep for ".required" - should have color and styling defined</verify>
  <done>Required field indicators standardized</done>
</task>

<task type="auto">
  <name>Task 3: Unify form-group structure</name>
  <files>public/css/styles.css</files>
  <action>
Consolidate form-group definitions:

1. Find all .form-group definitions
2. Create unified base styling:

```css
/* Form group - standard field wrapper */
.form-group {
  margin-bottom: 1rem;
}

.form-group:last-child {
  margin-bottom: 0;
}

/* Form group in compact contexts */
.form-group-compact,
.form-row .form-group {
  margin-bottom: 0;
}
```

3. Ensure form-group works consistently in all contexts (modals, panels, standalone)
  </action>
  <verify>Grep for "form-group {" - should have single unified definition</verify>
  <done>Form group structure unified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Labels have consistent font-size (0.8125rem)
- [ ] Labels have consistent margin-bottom (0.375rem)
- [ ] .required class has red color styling
- [ ] .form-group has consistent margin-bottom
- [ ] No CSS syntax errors
</verification>

<success_criteria>
- All form labels share consistent styling
- Required indicators visually consistent
- Form groups have uniform structure
- Works in both light and dark contexts
</success_criteria>

<output>
After completion, create `.planning/phases/34-forms-validation/34-01-SUMMARY.md`
</output>
