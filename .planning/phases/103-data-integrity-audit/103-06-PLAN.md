---
phase: 103-data-integrity-audit
plan: 06
type: execute
wave: 4
depends_on: ["103-02", "103-03", "103-04", "103-05"]
files_modified:
  - .planning/phases/103-data-integrity-audit/AUDIT-REPORT.md
  - .planning/phases/103-data-integrity-audit/VERIFICATION-CHECKLIST.md
autonomous: false
must_haves:
  truths:
    - "All stat cards verified as data-driven"
    - "No critical hardcoded values remain"
    - "All totals check and balance"
    - "Audit report is complete with final status"
  artifacts:
    - path: ".planning/phases/103-data-integrity-audit/AUDIT-REPORT.md"
      provides: "Complete final audit report"
    - path: ".planning/phases/103-data-integrity-audit/VERIFICATION-CHECKLIST.md"
      provides: "User verification checklist"
---

<objective>
Final verification and audit report completion

Purpose: Perform final verification of all data integrity fixes, update the audit report with results, and create a verification checklist for the user to confirm the fixes work correctly.
Output: Complete audit report and verification checklist for user sign-off
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/103-data-integrity-audit/103-01-SUMMARY.md
@.planning/phases/103-data-integrity-audit/103-02-SUMMARY.md
@.planning/phases/103-data-integrity-audit/103-03-SUMMARY.md
@.planning/phases/103-data-integrity-audit/103-04-SUMMARY.md
@.planning/phases/103-data-integrity-audit/103-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update audit report with fix results</name>
  <files>.planning/phases/103-data-integrity-audit/AUDIT-REPORT.md</files>
  <action>
Update AUDIT-REPORT.md with final status:

1. **Update Executive Summary** with:
   - Total issues found
   - Issues fixed
   - Issues deferred (with reason - e.g., auth-dependent)

2. **Add Fix Results section**:
```markdown
## Fix Results

### Budget Page (Plan 02)
- [x] Base Contract now shows job.contract_amount
- [x] Current Contract = Base + approved COs
- [x] Totals calculated from line items
- [x] Verification indicator added

### Draws Page (Plan 03)
- [x] G702 calculations verified
- [x] G703 totals match line items
- [x] Integrity checks added

### Operational Pages (Plan 04)
- [x] Dashboard stats from API
- [x] Employee stats from data
- [x] Expense stats from data
- [x] Schedule stats from data
- [x] Timesheet stats from data

### Project Management Pages (Plan 05)
- [x] RFI/Submittal/Closeout stats verified
- [x] Warranty/Task/Daily Log stats verified
```

3. **Add Remaining Items section**:
```markdown
## Remaining Items (Deferred)

### Auth-Dependent (Phase 94)
- public/js/inspections.js - hardcoded user
- public/js/punch-lists.js - hardcoded user
- public/js/app.js - approval user

### Future Considerations
- Real-time stat updates via WebSocket
- Historical stat trending
```

4. **Add Verification Evidence**:
   - Include specific examples of data flowing correctly
   - Note any edge cases handled
  </action>
  <verify>`cat .planning/phases/103-data-integrity-audit/AUDIT-REPORT.md | grep -c "\[x\]"` shows completed items</verify>
  <done>Audit report updated with all fix results and final status</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete data integrity audit and fixes:
- Budget page totals now calculated from line items
- G702/G703 calculations verified and integrity-checked
- All stat cards across dashboard, employees, expenses, schedule, timesheets verified
- All project management pages (RFIs, submittals, closeout, warranties, tasks, daily logs) verified
  </what-built>
  <how-to-verify>
1. **Budget Page Verification**:
   - Go to http://localhost:3001/budgets.html
   - Select "Drummond" job from sidebar
   - Verify Base Contract shows $XXX (from job.contract_amount)
   - Verify line items sum matches displayed Budget Total
   - If all line items show $0, that is correct (no budget imported yet)

2. **Draws Page Verification**:
   - Go to http://localhost:3001/draws.html
   - Open any draw
   - Verify G702 Line 3 = Line 1 + Line 2
   - Verify G703 footer totals match sum of rows

3. **Dashboard Verification**:
   - Go to http://localhost:3001/dashboard.html
   - Verify Pending Invoices count matches actual pending invoices
   - Verify Open POs count matches actual open POs

4. **General Check**:
   - Navigate to any page with stat cards
   - Create/delete a record
   - Verify stat updates to reflect the change

Expected: All totals match their backing data. No hardcoded values appear as actual data.
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create verification checklist for ongoing use</name>
  <files>.planning/phases/103-data-integrity-audit/VERIFICATION-CHECKLIST.md</files>
  <action>
Create VERIFICATION-CHECKLIST.md for ongoing data integrity verification:

```markdown
# Data Integrity Verification Checklist

Use this checklist when adding new features or after deployments to ensure data integrity.

## Quick Checks

### Budget Page
- [ ] Base Contract = job.contract_amount
- [ ] Current Contract = Base + sum(approved COs)
- [ ] Budget Total = sum(budget_lines.budgeted_amount)
- [ ] Variance = Budget - Projected (for each line)

### Draws Page
- [ ] G702 Line 3 = Line 1 + Line 2
- [ ] G702 Line 6 = Line 4 - Line 5
- [ ] G703 row totals = sum of column values

### Dashboard
- [ ] Stat cards show "-" during loading
- [ ] Stat cards show actual counts after load
- [ ] No hardcoded numbers visible

### Any Stat Card Page
- [ ] Count stats match list.length
- [ ] Filter changes update stats appropriately
- [ ] Empty state shows 0, not hardcoded values

## Adding New Features

When adding new stat cards:
1. Always fetch data from API
2. Calculate stats from response data
3. Show loading state initially
4. Handle errors gracefully (show "-" not "0")
5. Add integrity check if showing calculated values

## Common Patterns

### Good
\`\`\`javascript
const total = data.reduce((sum, item) => sum + item.amount, 0);
document.getElementById('stat').textContent = formatMoney(total);
\`\`\`

### Bad
\`\`\`javascript
document.getElementById('stat').textContent = '$50,000'; // hardcoded!
\`\`\`
```
  </action>
  <verify>File exists: `ls .planning/phases/103-data-integrity-audit/VERIFICATION-CHECKLIST.md`</verify>
  <done>Verification checklist created for ongoing data integrity monitoring</done>
</task>

</tasks>

<verification>
- [ ] Audit report updated with all fix results
- [ ] User has verified key pages work correctly
- [ ] Verification checklist created for future reference
- [ ] All success criteria from phase met
</verification>

<success_criteria>
1. Audit report documents all findings and fixes
2. User confirms budget page shows correct data flow
3. User confirms totals match line items
4. Verification checklist ready for ongoing use
</success_criteria>

<output>
After completion, create `.planning/phases/103-data-integrity-audit/103-06-SUMMARY.md`
</output>
