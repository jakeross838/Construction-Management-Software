---
phase: 103-data-integrity-audit
plan: 05
type: execute
wave: 3
depends_on: ["103-01"]
files_modified:
  - public/rfis.html
  - public/js/rfis.js
  - public/submittals.html
  - public/js/submittals.js
  - public/closeout.html
  - public/js/closeout.js
  - public/warranties.html
  - public/js/warranties.js
  - public/tasks.html
  - public/js/tasks.js
  - public/daily-logs.html
  - public/js/daily-logs.js
autonomous: true
must_haves:
  truths:
    - "RFI stats match actual RFI counts by status"
    - "Submittal stats match actual submittal counts by status"
    - "Closeout stats match actual closeout item counts"
    - "Warranty stats match actual warranty counts"
    - "Task stats match actual task counts"
    - "Daily log stats match actual log data"
  artifacts:
    - path: "public/rfis.html"
      provides: "Data-driven RFI stats"
    - path: "public/submittals.html"
      provides: "Data-driven submittal stats"
    - path: "public/tasks.html"
      provides: "Data-driven task stats"
---

<objective>
Fix project management pages data integrity - RFIs, submittals, closeout, warranties, tasks, daily logs

Purpose: Verify and fix data integrity for project management pages that track items by status (open, pending, answered, closed), ensuring all displayed counts come from database queries.
Output: All project management stat cards backed by live database queries
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/103-data-integrity-audit/103-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix RFIs, Submittals, Closeout stat cards</name>
  <files>public/rfis.html, public/js/rfis.js, public/submittals.html, public/js/submittals.js, public/closeout.html, public/js/closeout.js</files>
  <action>
Audit each page's stat cards and fix data loading:

1. **RFIs page** stat cards:
   - Total: COUNT from v2_rfis
   - Open: COUNT WHERE status='open'
   - Pending: COUNT WHERE status='pending_response'
   - Answered: COUNT WHERE status='answered'
   - Closed: COUNT WHERE status='closed'

2. **Submittals page** stat cards:
   - Total: COUNT from v2_submittals
   - Pending: COUNT WHERE status='pending'
   - Open/Under Review: COUNT WHERE status='under_review'
   - Approved: COUNT WHERE status='approved'
   - Rejected/Closed: COUNT WHERE status IN (...)

3. **Closeout page** stat cards:
   - Total Items
   - Answered/Completed
   - Pending
   - Closed
   - Open

For each page:
a. Find the stat card update function (likely called after loading data)
b. Verify stats are calculated from the loaded data array, not hardcoded
c. Ensure stats update when filters change (show filtered counts or total counts consistently)

Pattern:
```javascript
function updateStats(items) {
  const byStatus = items.reduce((acc, item) => {
    acc[item.status] = (acc[item.status] || 0) + 1;
    return acc;
  }, {});

  document.getElementById('statTotal').textContent = items.length;
  document.getElementById('statOpen').textContent = byStatus['open'] || 0;
  document.getElementById('statPending').textContent = byStatus['pending'] || 0;
  // etc.
}
```

Fix any patterns like:
```javascript
// BAD
document.getElementById('statTotal').textContent = '0'; // hardcoded
// GOOD
document.getElementById('statTotal').textContent = data.length;
```
  </action>
  <verify>
- Load each page with actual data
- Verify stat card counts match list counts
- Filter by status and verify filtered stat reflects selection
  </verify>
  <done>RFI, Submittal, and Closeout pages have data-driven stat cards</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix Warranties, Tasks, Daily Logs stat cards</name>
  <files>public/warranties.html, public/js/warranties.js, public/tasks.html, public/js/tasks.js, public/daily-logs.html, public/js/daily-logs.js</files>
  <action>
Audit each page's stat cards:

1. **Warranties page** stat cards:
   - Total
   - Active (answered status)
   - Expiring Soon (pending status)
   - Expired (closed status)
   - Claimed (open status)

2. **Tasks page** stat cards:
   - Total
   - Open
   - In Progress (pending)
   - Completed (answered)
   - Overdue (closed)

3. **Daily Logs page** stat cards:
   - Total logs count
   - Crew count
   - Weather conditions
   - Issues/Notes count

For each page:
a. Verify data loading function
b. Ensure stats computed from actual data
c. Fix any hardcoded fallbacks

Additionally check for hardcoded user references:
- daily-logs.js may have hardcoded user for "created_by"
- Ensure these are clearly marked with TODO comments for future auth

Pattern for handling missing auth:
```javascript
// TODO: Replace with authenticated user when auth is implemented
const currentUser = 'Jake Ross';
```
  </action>
  <verify>
- Load each page with test data
- Verify counts match actual record counts
- Check console for any hardcoded value warnings
  </verify>
  <done>Warranties, Tasks, and Daily Logs pages have data-driven stat cards</done>
</task>

</tasks>

<verification>
- [ ] RFI stats match actual RFI counts
- [ ] Submittal stats match actual submittal counts
- [ ] Closeout stats match actual item counts
- [ ] Warranty stats match actual warranty counts
- [ ] Task stats match actual task counts
- [ ] Daily log stats reflect actual log entries
- [ ] All hardcoded user references marked with TODO
</verification>

<success_criteria>
1. All project management pages calculate stats from loaded data
2. No hardcoded numeric values in stat displays
3. Stats update correctly when data changes
4. Hardcoded user references documented for future auth integration
</success_criteria>

<output>
After completion, create `.planning/phases/103-data-integrity-audit/103-05-SUMMARY.md`
</output>
