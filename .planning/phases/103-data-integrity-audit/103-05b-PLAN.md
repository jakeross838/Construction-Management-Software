---
phase: 103-data-integrity-audit
plan: 05b
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - public/warranties.html
  - public/js/warranties.js
  - public/tasks.html
  - public/js/tasks.js
  - public/daily-logs.html
  - public/js/daily-logs.js
autonomous: true
must_haves:
  truths:
    - "Warranty stats match actual warranty counts"
    - "Task stats match actual task counts"
    - "Daily log stats match actual log data"
  artifacts:
    - path: "public/warranties.html"
      provides: "Data-driven warranty stats"
    - path: "public/tasks.html"
      provides: "Data-driven task stats"
    - path: "public/daily-logs.html"
      provides: "Data-driven daily log stats"
  key_links:
    - from: "public/warranties.html"
      to: "/api/warranties"
      via: "fetch on page load"
      pattern: "api/warranties"
---

<objective>
Fix project management pages data integrity - warranties, tasks, daily logs

Purpose: Verify and fix data integrity for warranties, tasks, and daily logs pages that track items by status, ensuring all displayed counts come from database queries.
Output: Warranty, task, and daily log stat cards backed by live database queries
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/103-data-integrity-audit/103-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix Warranties and Tasks stat cards</name>
  <files>public/warranties.html, public/js/warranties.js, public/tasks.html, public/js/tasks.js</files>
  <action>
Audit each page's stat cards:

1. **Warranties page** stat cards:
   - Total
   - Active (answered status)
   - Expiring Soon (pending status)
   - Expired (closed status)
   - Claimed (open status)

2. **Tasks page** stat cards:
   - Total
   - Open
   - In Progress (pending)
   - Completed (answered)
   - Overdue (closed)

For each page:
a. Verify data loading function
b. Ensure stats computed from actual data
c. Fix any hardcoded fallbacks

Pattern:
```javascript
function updateWarrantyStats(items) {
  const byStatus = items.reduce((acc, item) => {
    acc[item.status] = (acc[item.status] || 0) + 1;
    return acc;
  }, {});

  document.getElementById('statTotal').textContent = items.length;
  document.getElementById('statActive').textContent = byStatus['answered'] || 0;
  document.getElementById('statExpiring').textContent = byStatus['pending'] || 0;
  document.getElementById('statExpired').textContent = byStatus['closed'] || 0;
  document.getElementById('statClaimed').textContent = byStatus['open'] || 0;
}
```
  </action>
  <verify>
- Load warranties page with test data
- Verify counts match actual record counts
- Load tasks page and verify same
  </verify>
  <done>Warranties and Tasks pages have data-driven stat cards</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix Daily Logs stat cards</name>
  <files>public/daily-logs.html, public/js/daily-logs.js</files>
  <action>
Audit daily logs page stat cards:

1. **Daily Logs page** stat cards:
   - Total logs count
   - Crew count
   - Weather conditions
   - Issues/Notes count

2. **Verify data loading and stats**:
   a. Ensure stats computed from actual data
   b. Fix any hardcoded fallbacks

3. **Check for hardcoded user references**:
   - daily-logs.js may have hardcoded user for "created_by"
   - Ensure these are clearly marked with TODO comments for future auth

Pattern for handling missing auth:
```javascript
// TODO: Replace with authenticated user when auth is implemented
const currentUser = 'Jake Ross';
```

Pattern for stats:
```javascript
function updateDailyLogStats(logs) {
  document.getElementById('totalLogs').textContent = logs.length;

  const uniqueCrews = new Set(logs.flatMap(l => l.crew || []));
  document.getElementById('crewCount').textContent = uniqueCrews.size;

  const issueCount = logs.reduce((sum, l) => sum + (l.issues?.length || 0), 0);
  document.getElementById('issueCount').textContent = issueCount;
}
```
  </action>
  <verify>
- Load daily logs page with test data
- Verify counts match actual record counts
- Check console for any hardcoded value warnings
  </verify>
  <done>Daily Logs page has data-driven stat cards with documented auth TODOs</done>
</task>

</tasks>

<verification>
- [ ] Warranty stats match actual warranty counts
- [ ] Task stats match actual task counts
- [ ] Daily log stats reflect actual log entries
- [ ] All hardcoded user references marked with TODO
</verification>

<success_criteria>
1. Warranties, Tasks, and Daily Logs pages calculate stats from loaded data
2. No hardcoded numeric values in stat displays
3. Stats update correctly when data changes
4. Hardcoded user references documented for future auth integration
</success_criteria>

<output>
After completion, create `.planning/phases/103-data-integrity-audit/103-05b-SUMMARY.md`
</output>
