---
phase: 33-tables-lists
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/styles.css
autonomous: true
must_haves:
  truths:
    - "All table classes have consistent base styling"
    - "data-table is the standard class with variants"
    - "Legacy table classes aliased for backward compatibility"
  artifacts:
    - path: "public/css/styles.css"
      provides: "Unified table CSS with aliases"
  key_links: []
---

<objective>
Standardize table CSS classes and create unified styling foundation.

Purpose: Create consistent CSS foundation for all tables before updating HTML/JS.
Output: Unified table CSS with legacy class aliases for backward compatibility.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-STANDARDS.md
@public/css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified data-table base class</name>
  <files>public/css/styles.css</files>
  <action>
Find the existing `.data-table` definition and ensure it has complete base styling. Then create aliases for legacy table classes to inherit from data-table:

1. Find `.data-table` styles (around line 764-815)
2. Create unified selectors that include legacy classes:

```css
/* Unified data table styling - all table types inherit this */
.data-table,
.invoices-table,
.history-table,
.comparison-table {
  width: 100%;
  border-collapse: collapse;
  background: var(--card);
  border-radius: var(--radius-lg);
  overflow: hidden;
}
```

3. Create unified header styling:

```css
.data-table th,
.invoices-table th,
.history-table th,
.comparison-table th {
  padding: 0.625rem 0.75rem;
  text-align: left;
  font-weight: 500;
  color: var(--muted-foreground);
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.025em;
  border-bottom: 1px solid var(--border);
  white-space: nowrap;
  background: var(--card-elevated);
}
```

4. Create unified row styling:

```css
.data-table td,
.invoices-table td,
.history-table td,
.comparison-table td {
  padding: 0.625rem 0.75rem;
  border-bottom: 1px solid var(--border);
  font-size: 0.875rem;
}
```

Note: Keep g702-table, g703-table, worksheet-table, and schedule-table separate as they have specialized financial/layout requirements.
  </action>
  <verify>Grep for "invoices-table" in styles.css - should be in unified selector</verify>
  <done>Legacy table classes aliased to data-table base styling</done>
</task>

<task type="auto">
  <name>Task 2: Standardize table hover states</name>
  <files>public/css/styles.css</files>
  <action>
Unify hover state styling across all aliased table types:

1. Find hover state definitions for each table type
2. Create unified hover selector:

```css
/* Unified table row hover */
.data-table-hover tbody tr:hover,
.invoices-table tbody tr:hover,
.history-table tbody tr:hover,
.comparison-table tbody tr:hover {
  background: var(--accent);
  cursor: pointer;
}
```

3. Ensure transition is consistent:

```css
.data-table tbody tr,
.invoices-table tbody tr,
.history-table tbody tr,
.comparison-table tbody tr {
  transition: background 0.15s ease;
}
```
  </action>
  <verify>Grep for "invoices-table tbody tr:hover" - should be in unified selector</verify>
  <done>Table hover states unified across all aliased types</done>
</task>

<task type="auto">
  <name>Task 3: Standardize action column styling</name>
  <files>public/css/styles.css</files>
  <action>
Create consistent action column styling:

1. Find existing `.col-actions` styles
2. Ensure unified definition:

```css
/* Table action column - standard for all tables */
.col-actions {
  width: 80px;
  text-align: right;
  white-space: nowrap;
}

.col-actions .btn {
  padding: 0.25rem 0.5rem;
}

.col-actions .btn + .btn {
  margin-left: 0.25rem;
}
```

3. Add row action button styling:

```css
/* Row action buttons - ghost style for inline actions */
.data-table .col-actions .btn-ghost,
.invoices-table .col-actions .btn-ghost,
.history-table .col-actions .btn-ghost {
  opacity: 0.6;
  transition: opacity 0.15s;
}

.data-table tbody tr:hover .col-actions .btn-ghost,
.invoices-table tbody tr:hover .col-actions .btn-ghost,
.history-table tbody tr:hover .col-actions .btn-ghost {
  opacity: 1;
}
```
  </action>
  <verify>Grep for "col-actions" - should have consistent styling</verify>
  <done>Action column styling standardized</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] .data-table has unified base styling
- [ ] .invoices-table, .history-table, .comparison-table aliased
- [ ] Hover states consistent across table types
- [ ] .col-actions has unified styling
- [ ] No CSS syntax errors
</verification>

<success_criteria>
- All standard table classes share base styling
- Legacy classes work identically to data-table
- Specialized tables (g702, g703, worksheet, schedule) unchanged
- Action columns consistent
</success_criteria>

<output>
After completion, create `.planning/phases/33-tables-lists/33-01-SUMMARY.md`
</output>
